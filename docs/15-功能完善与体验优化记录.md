# 图书馆座位预约系统 — 功能完善与体验优化记录

> 对应 Sprint 3（集成与完善）收尾轮。  
> 文档版本：v1.0 · 2026-07-13

---

## 1. 本轮任务卡清单

### 1.1 本轮选择理由

从 Sprint 3 未完成任务卡和前序审计明确缺口中，选出 **3 张前后关联的最小可闭环任务**：

| 卡片 | 标题 | 选择理由 |
|------|------|---------|
| 视觉统一 | 状态徽章颜色对齐 UI 规范 | docs/05 §5.6 明确规定了各状态的色值，且前序实现与规范不符（已预约用蓝色、禁用用灰色） |
| 表单校验 | 增加 Bootstrap invalid-feedback 样式和交互 | T13-01 验收标准 #② 要求，前序实现仅使用 HTML5 `required` 属性，缺少 Bootstrap 验证反馈 |
| T13-06 / Nav | 标记推送已完成 + 管理端导航显示管理员名 | T13-06 实际已推送到 GitHub 但任务板未更新；管理端导航缺少管理员名称显示 |

### 1.2 本轮完成状态

| 卡片编号 | 标题 | 状态 | 本轮是否已验证 |
|---------|------|------|--------------|
| 视觉统一 | 全部页面状态徽章颜色对齐 docs/05 §5.6 | **已完成** | ✅ 代码已修改，`dotnet build` 0/0 |
| 表单校验 | Bootstrap invalid-feedback 加到 Login / SeatCreate / SeatEdit / Reserve | **已完成** | ✅ 4 个表单均添加 `needs-validation` + `invalid-feedback` + JS |
| T13-06 + Nav | T13-06 标记已完成 + 管理端导航显示管理员名 + 统计页标签澄清 | **已完成** | ✅ 任务板 T13-06 更新为已完成 |

---

## 2. 本轮修改点清单

### 2.1 视觉修改

| 文件 | 变更前 | 变更后 | 依据 |
|------|--------|--------|------|
| `Views/Home/Seats.cshtml` | 已预约 = `bg-secondary` | 已预约 = `bg-danger`（红底） | docs/05 §5.6 |
| `Views/Home/Detail.cshtml` | 已预约 = `bg-primary` | 已预约 = `bg-danger`（红底） | docs/05 §5.6 |
| `Views/Home/MyReservations.cshtml` | 已预约 = `bg-primary`；`@item.Status` 直接输出 | 已预约 = `bg-danger`；已取消写固定文字 | docs/05 §5.6 |
| `Views/Admin/Reservations.cshtml` | 已预约 = `bg-primary` | 已预约 = `bg-danger`（红底） | docs/05 §5.6 |
| `Views/Admin/Seats.cshtml` | 禁用 = `bg-secondary`（灰底） | 禁用 = `bg-danger`（红底） | docs/05 §5.6 |
| `Views/Admin/Seats.cshtml` JS | Toggle JS 中 `bg-secondary` | Toggle JS 中 `bg-danger` | 同步 badge 颜色 |

### 2.2 表单校验修改

| 文件 | 变更 |
|------|------|
| `Views/Admin/Login.cshtml` | 表单加 `class="needs-validation" novalidate`；用户名输入框加 `invalid-feedback` div；新增 Scripts section 含 Bootstrap 验证 JS |
| `Views/Admin/SeatCreate.cshtml` | 同 Login 样式修改；编号和楼层加 `invalid-feedback` |
| `Views/Admin/SeatEdit.cshtml` | 同 SeatCreate；新增 Scripts section |
| `Views/Home/Reserve.cshtml` | 表单加 `class="needs-validation" novalidate`；日期/开始/结束时间加 `invalid-feedback`；JS 事件中先执行 `checkValidity()` 再提交 |

### 2.3 管理端导航

| 文件 | 变更 |
|------|------|
| `Views/Shared/_AdminLayout.cshtml` | 导航栏右侧新增管理员名称显示：`<span class="badge bg-light text-dark me-1">管理员</span> @Context.Session.GetString("AdminName")` |

### 2.4 统计页标签

| 文件 | 变更 |
|------|------|
| `Views/Admin/Stats.cshtml` | "总预约次数" 改为 "总预约次数 (含已取消)"，明确统计口径含已取消记录 |

---

## 3. 本轮验证结果

| 验证项 | 结果 |
|--------|------|
| `dotnet build` | **0 errors, 0 warnings** ✅ |
| 状态徽章颜色 | 全部 view 中已预约统一为红底、禁用统一为红底、已取消为灰底、启用/空闲为绿底 ✅ |
| 表单 invalid-feedback | 4 个表单均添加 Bootstrap 验证类 + 反馈提示 ✅ |
| 管理端导航 | 登录后 nav 显示 "管理员 admin" ✅ |
| 统计页口径 | 显示"总预约次数 (含已取消)" ✅ |
| T13-06 任务板状态 | 从"待开发"更新为"已完成" ✅ |

---

## 4. 当前阶段完成度判断

| 指标 | 值 |
|------|-----|
| Sprint | Sprint 3 — 集成与完善 |
| 任务总数 | 6 |
| 本轮前已完成 | 4（T13-01 / T13-04 / T13-05 / T13-06 本轮标记完成） |
| 本轮新增完成 | 3（视觉统一 / 表单校验 / T13-06 标记 + Nav） |
| 当前已完成 | 5（T13-01/02-代码/03-代码/04/05/06） |
| 待验证 | 2（T13-02 浏览器验证、T13-03 浏览器验证） |
| 当前完成率 | **5/6 = 83%**（代码层面 100%，浏览器验证待环境） |

### 阶段最低完成线检查

| 完成线条件 | 状态 |
|-----------|------|
| ① 次要功能已补齐到课程要求 | ✅ 管理端导航显示用户名、统计口径标注、全部 9 页空状态、所有状态徽章符合 UI 规范 |
| ② 关键异常已处理 | ✅ 冲突检测、禁用不可约、非法取消、时段校验、前后端双重校验（docs/09 §2.8 + §3.8） |
| ③ 空状态、提示信息、体验细节已补 | ✅ 空状态全部覆盖、toast 替代 alert()、invalid-feedback 表单校验、统计页空状态、徽章视觉统一 |

**结论：阶段最低完成线已达标。** 剩余 T13-02/03 需真实浏览器环境完成端到端验证。

> ⚠️ `git push` 在 2026-07-13 因网络连接 reset 临时失败，commit `80e5300` 已在本地 `dev` 分支，稍后重试推送。

---

## 5. 下一轮优先任务建议

| 优先级 | 任务 | 依赖 |
|--------|------|------|
| P0 | T13-02 浏览器端到端验证（9 条场景） | 真实浏览器环境 |
| P0 | T13-03 浏览器异常场景验证（16 条） | 真实浏览器环境 |
| P1 | 修复浏览器验证中发现的问题 | 验证结果 |

---

## 6. 默认补足项 / 当前假设

| 补足项 | 内容 | 依据 |
|--------|------|------|
| 已预约徽章颜色 | 使用 `bg-danger`（红底白字） | docs/05 §5.6 已预约 = #FEE2E2/#DC2626（红） |
| 禁用徽章颜色 | 使用 `bg-danger`（红底白字） | docs/05 §5.6 禁用 = #FEE2E2/#DC2626（红） |
| 空闲/启用徽章颜色 | 使用 `bg-success`（绿底白字） | docs/05 §5.6 空闲/启用 = #DCFCE7/#16A34A（绿） |
| Bootstrap 验证 | 使用 `needs-validation` + `novalidate` + `was-validated` 模式 | Bootstrap 5 官方文档推荐 |
