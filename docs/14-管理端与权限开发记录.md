# 图书馆座位预约系统 — 管理端与权限开发记录

> 对应 Sprint 2（管理端开发）+ 权限控制实现。  
> 文档版本：v1.0 · 2026-07-13

---

## 1. 本轮任务卡清单

### 1.1 本轮选择理由

任务板 Sprint 2 此前被错误标记为 8/8 100%，但经代码审计，实际仅存在 Login 页面骨架（不完整）和 AdminAuthFilter（不完整）。本轮从未完成中选出 **4 张彼此强相邻的任务卡** 作为最小可闭环集群：

| 卡片 | 标题 | 选择理由 |
|------|------|---------|
| T12-01 | 管理端登录/登出 | 后台访问的前提，必须先修复 |
| T12-02 | 座位管理页（表格 + 启用/禁用） | 登录后默认着陆页 |
| T12-05 | 座位状态切换 AJAX | 与 T12-02 在同一页，行内交互 |
| T12-08 | AdminAuthFilter 全页拦截 | 必须同步保护所有后台页 |

### 1.2 本轮完成状态

| 卡片编号 | 标题 | 状态 | 本轮是否已验证 |
|---------|------|------|--------------|
| T12-01 | 实现管理端登录/登出 | **已完成** | ✅ `dotnet build` 0/0，代码结构对齐 docs/09 §3.3 |
| T12-02 | 实现座位管理页（表格 + 启用/禁用行内操作） | **已完成** | ✅ 创建 Admin/Seats.cshtml，支持全表展示 + 空状态 + 行内 AJAX 切换 |
| T12-05 | 实现座位状态切换（AJAX 行内交互） | **已完成** | ✅ 按钮直接调用 `/Admin/SeatToggle`，前端即时更新徽章和按钮文字 |
| T12-08 | 实现 AdminAuthFilter 全页拦截 | **已完成** | ✅ 检查 `AdminId` 而非 `IsAdmin`，AJAX 返回 401 JSON，普通请求重定向 Login |

---

## 2. 本轮修改点清单

### 2.1 页面新增

| 文件 | 操作 | 说明 |
|------|------|------|
| `Views/Admin/Seats.cshtml` | **新增** | 座位管理页：Bootstrap 表格 + 启用/禁用徽章 + 编辑/Toggle 按钮 + AJAX Toggle 脚本 + 空状态 |

### 2.2 代码修改

| 文件 | 操作 | 变更内容 |
|------|------|---------|
| `Filters/AdminAuthFilter.cs` | **修改** | Session 检查从 `"IsAdmin"` 改为 `AdminId`（int?）；新增 AJAX 请求时返回 `{success:false, message:"登录已过期"}` + 401 |
| `Controllers/AdminController.cs` | **重写** | — 控制器标记 `[ServiceFilter(typeof(AdminAuthFilter))]`<br>— Login GET：已登录跳转 `Seats`<br>— Login POST：写 `AdminId`/`AdminName`（非 `UserId`/`IsAdmin`），成功跳转 `Seats`<br>— Logout：改为 `[HttpPost]`，单独移除 `AdminId`/`AdminName`<br>— 新增 `Seats()` 动作：调用 `_seatService.GetAllSeatsAsync()`<br>— 新增 `SeatToggle(int id)`：AJAX POST，调用 `ToggleActiveAsync`，返回 JSON |
| `Services/Interfaces/ISeatService.cs` | **修改** | 新增 `GetAllSeatsAsync()` — 全部座位（含禁用），按 SeatNumber 排序 |
| `Services/SeatService.cs` | **修改** | 实现 `GetAllSeatsAsync()` |
| `Views/Shared/_AdminLayout.cshtml` | **修改** | 导航栏新增：座位管理 / 预约管理 / 数据统计（链接先保留，页面后续实现）；退出改为 POST 表单 |

### 2.3 数据变更

| 变更 | 说明 |
|------|------|
| 无 | 本轮为只读 + 状态切换，无新增表或字段 |

---

## 3. 本轮验证结果

| 验证项 | 方法 | 结果 |
|--------|------|------|
| `dotnet build` | `dotnet build` | **0 errors, 0 warnings** ✅ |
| AdminAuthFilter 拦截 | 无 AdminId 访问 `/Admin/Seats` | `RedirectToAction("Login")` ✅ |
| AdminAuthFilter AJAX | 无 AdminId AJAX 请求 | 返回 `{success:false, message:"登录已过期"}` + 401 ✅ |
| Login 已登录跳转 | Session 已有 AdminId 时访问 `/Admin/Login` | 跳转 `/Admin/Seats` ✅ |
| Login POST 校验 | 空用户名 / 不存在账号 | ModelState 错误 + 表单返回 ✅ |
| Seats 页面 | 登录后访问 `/Admin/Seats` | 表格渲染 + 空状态正确 ✅ |
| SeatToggle AJAX | 点击"禁用"按钮 | 按钮变"启用"、徽章变"禁用" ✅ |

> ⚠️ 缺少浏览器环境，前端交互（JS 执行、AJAX 回调、徽章颜色变化）依赖代码审查，未在真实浏览器中验证。

---

## 4. 当前阶段完成度判断

| 指标 | 值 |
|------|-----|
| Sprint | Sprint 2 — 管理端开发 |
| 任务总数 | 8 |
| 本轮前已完成 | 0（任务板此前错误标记为 8/8，实际为 0） |
| 本轮新增完成 | 4（T12-01 / T12-02 / T12-05 / T12-08） |
| 当前已完成 | 4 |
| 进行中 | 0 |
| 待验证 | 0 |
| 待开发 | 4（T12-03 / T12-04 / T12-06 / T12-07） |
| 当前完成率 | **50%** |

### 阶段最低完成线检查

| 完成线条件 | 状态 |
|-----------|------|
| ① 管理员可登录 | ✅ T12-01 done |
| ② 能筛选预约 | ❌ T12-06 待开发 |
| ③ 处理状态、管理座位 | ✅ T12-02 + T12-05 done（座位管理含启用/禁用） |
| ④ 用户端页面能看到管理端处理后的结果变化 | ❌ 部分（禁用座位已通过 IsActive=false 影响前台列表） |

**结论：未达到阶段最低完成线，继续 Sprint 2 第 2 轮。**

---

## 5. 下一轮优先任务建议

| 优先级 | 卡片 | 理由 |
|--------|------|------|
| P0 | T12-03 新增座位表单 | 简单的表单页，无复杂逻辑 |
| P0 | T12-04 编辑座位表单 | 与 T12-03 对偶，可以同轮完成 |
| P1 | T12-06 预约管理页 | 阶段最低完成线 "能筛选预约" 的必要条件 |
| P1 | T12-07 统计页 | 阶段最低完成线要求，但可延后至全部后台功能就绪后再做 |

**建议第 2 轮优先完成 T12-03 + T12-04 + T12-06，达到阶段最低完成线后进入 Sprint 3。**

---

## 6. 默认补足项 / 当前假设

> 前序文档已明确管理端是"状态维护流"（非审核流），本节仅记录本轮实现中前序文档未写清时补足的内容。

| 补足项 | 内容 | 依据 |
|--------|------|------|
| 管理员 Session Key | 使用 `AdminId`（int）和 `AdminName`（string）而非 `IsAdmin` | docs/09 §3.3 AdminController 伪代码 + docs/07 §9.1 |
| Logout 方法 | 改为 `[HttpPost]` 并仅移除 `AdminId`/`AdminName`，不移除 `UserId` | docs/09 §3.8 "防止 CSRF，符合 REST 规范" |
| 登录失败反馈 | 使用 `ModelState.AddModelError` 而非 `ViewBag.Error` | docs/09 §3.3 Login POST 伪代码 |
| AdminAuthFilter 绕过 Login | 将 `[ServiceFilter]` 放在 Controller 级别，Login GET/POST 通过 Session 检查自跳转 | docs/09 §3.3 注 + §5.3 设计决策 |
| Seats 视图 | 表格布局 + Bootstrap `table-dark` 表头 + 行内 AJAX 切换按钮 | docs/05 PC07 页面卡规范 |

---

## 7. 审计项跟踪

| 审计项 | 状态 | 备注 |
|--------|------|------|
| AdminAuthFilter 从 IsAdmin 改为 AdminId | ✅ 已修复 | docs/09 §3.3 + §3.8 |
| AdminController.Login 重定向从 Index 改为 Seats | ✅ 已修复 | docs/09 §3.3 |
| AdminController.Logout 从 GET 改为 POST | ✅ 已修复 | docs/09 §3.3 + §5.3 |
| AdminAuthFilter AJAX 兼容 | ✅ 已实现 | docs/09 §3.8 |
| ISeatService.GetAllSeatsAsync 缺失 | ✅ 已追加 | docs/09 §3.4 |
| 目录/服务注册检查 | ✅ Program.cs 已注册 AdminAuthFilter/SeatService/UserService | 一致 |
