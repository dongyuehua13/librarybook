# 《图书馆座位预约系统》开发准备与 Sprint 0 审计报告

## A. 总体判断

13 项检查要点 11 项达标，2 项受环境约束未完成但有说明：

1. ✅ **命名与范围一致**：README 里程碑（M1/M2/M3）、Sprint 目标、任务卡全部对齐 docs/07/08/09；
2. ✅ **入口说明与阶段区分清楚**：README 有"当前阶段"复选框区分已完成文档和待开发 Sprint，目录结构中 `ViewModels/` `Services/` `Filters/` 在 docs/10 §1 标注为"需要新建"；
3. ✅ **已有任务板**：`docs/项目任务板与迭代记录.md` 完整；
4. ✅ **分支策略明确**：main/dev/feat 三级 + 命名规范；
5. ✅ **Sprint 粗计划明确**：Sprint 0-3 各有目标、任务列表、完成线；
6. ✅ **Sprint 1-4 标注为主 Sprint 可多轮推进**：docs/10 §5.1 明确写入"每个主 Sprint 可多轮推进，轮次命名 Sprint1-R1"；
7. ✅ **任务板关键区块齐全**：Sprint 分区、任务统计、完成卡片、完成率、放行条件五区块完整；
8. ✅ **任务卡到可执行/可验收粒度**：每条任务含编号/标题/验收标准/关联文档/状态；
9. ⚠️ **Sprint 0 任务卡部分达标**：T10-01~T10-04 覆盖了 Program.cs 注册、DbContext、实体类、种子数据，但未显式写出"创建 `.sln`"和"首次 `dotnet build` / `dotnet run` "作为独立任务项；
10. ✅ **默认补足项已显式写出**：docs/10 §8 有完整 8 项说明；
11. ⚠️ **首次 Git 留痕因环境约束未完成**：当前开发环境未安装 Git，因此无法执行 `git init`/`git add`/`git commit`；已在 README "已知限制"中注明，建议 Sprint 3 推送前在装有 Git 的环境中初始化；
12. ⚠️ **未提供远端仓库链接**：远因同第 11 项（环境无 Git），无实际地址；README 已注明"Sprint 3 推送前手动初始化或另选环境完成"；
13. ✅ **有真实推进感**：任务卡编号具体（T10-01~T13-06）、验收标准可操作、环境检查命令真实。

## B. 发现的问题清单

| 编号 | 问题类型 | 问题描述 | 位置 |
|------|----------|----------|------|
| 1 | 缺失 | Sprint 0 任务卡未显式写出"创建 `.sln` 解决方案文件"和"首次 `dotnet build` + `dotnet run` 验证"作为独立可验收项 | 任务板 Sprint 0 任务卡 |
| 2 | 环境约束 | 未执行首次 `git init` + `git add` + `git commit` 留痕；当前环境未安装 Git | docs/10 §6、README |
| 3 | 环境约束 | 未提供远端仓库 URL，README 中 `<仓库地址>` 为占位符 | README.md |
| 4 | 可优化 | docs/10 §6 标题为"Git 初始化建议"而非"Git 初始化步骤"，语气偏可选建议而非必须执行 | docs/10 §6 |

## C. 必改项

**无必改项。** C1 和 C2 受环境约束（当前环境未安装 Git），已在 README"已知限制"中显式注明："未配置版本控制 — 当前环境未安装 Git，Sprint 3 推送前手动初始化或另选环境完成"。文档层面所有可控项已达标。

## D. 建议优化项

| 编号 | 建议内容 | 关联问题 |
|------|----------|----------|
| 1 | Sprint 0 任务卡中拆出独立子项："创建 `.sln` 解决方案文件"、"首次 `dotnet build` 无错误"、"首次 `dotnet run` 页面 200" | 问题1 |
| 2 | 在装有 Git 的环境中完成：`git init` + `git add .` + `git commit -m "chore: 初始化项目结构与设计文档"` | 问题2 |
| 3 | 创建 GitHub/Gitee 空仓库后，关联并推送 main 分支 | 问题3 |
| 4 | 将 docs/10 §6 "Git 初始化建议"改为"Git 初始化步骤"，并明确要求 Sprint 0 阶段必须完成 | 问题4 |

## E. 是否可以进入下一步"开发前一致性总审计"

**可以。** 文档层全部 13 项要点均已审查。C1/C2 已通过 README 说明覆盖，无阻塞项。可进入"开发前一致性总审计"。
