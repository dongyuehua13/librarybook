# 《图书馆座位预约系统》功能完善、异常与体验优化 — 审计报告

> 审计日期：2026-07-13  
> 审计对象：README.md、docs/15-功能完善与体验优化记录.md、docs/项目任务板与迭代记录.md、src/  
> 对照基线：docs/03/06/07/08/09/11/13/14、任务板 Sprint 3 卡  
> 审计范围：仅审阶段15新增结果（Sprint 3 完整产出），不重做阶段14或更早阶段的全量审计

---

## A. 总体判断

**不通过。** 本轮存在 1 项 P0 阻塞项（关键文档缺失）、4 项 P1 未达标项，且阶段最低完成线"两条主链路在真实浏览器中完整走通无报错"因无浏览器环境**事实未达成**，不应误判可进入下一阶段。

---

## B. 13 项检查点逐项结果

### 1. 本轮补的内容是否优先来自前序文档、前序审计结果和任务板中的已确认缺口？

**大致对齐，但存在缺口遗漏。**

| 输入源 | 是否覆盖 | 说明 |
|--------|---------|------|
| 原型评审清单 (docs/06 审计) | ✅ T13-01 | 统计空状态 / toast / 校验样式 |
| docs/11 P1-03/P1-04 | ✅ T13-02 | AC02/AC16/QC03 + 3 条场景追加 |
| docs/13 审计（T13-04 前置） | ✅ T13-04 | 目录/实体/方法签名一致性 |
| docs/14 审计 P1-02/P1-03 | **❌ 未纳入本轮** | SeatToggle isActive + 行内 DOM（虽已在上一轮修复，但未在本轮记录中提及时机） |
| docs/14-审计 P0-01 (docs/14 缺失) | ✅ 已在上一轮修复 | docs/14 已创建 |
| README 事实错误 (net10.0/Migrations) | **❌ 遗漏** | 见检查点 9 |

### 2. 是否补上了关键次要功能？

**有补但未完全达标。**

| 功能 | 验收要求 | 实际状态 | 结论 |
|------|---------|---------|------|
| 统计页空状态 | 无数据时不显示空图表框架 | `@if (!Model.SeatRankings.Any())` "暂无数据" + `@if (Model.DailyTrends.Any())` + else "暂无预约数据" | ✅ |
| toast 替代 alert | 预约提交后 toast 提示 | Reserve.cshtml 使用 Bootstrap toast；MyReservations.cshtml 取消用 toast 显示错误 | ✅ |
| 表单校验样式 | invalid-feedback 样式正确展示 | Reserve.cshtml 仅使用 HTML5 `required`，未加 Bootstrap `was-validated` / `is-invalid` / `invalid-feedback` 样式 | **❌ 未实现** |
| XML 注释 | Service 接口方法补充 XML 注释 | 全部 Services/*.cs 中无任何 `/// <` 注释 | **❌ 未实现** |

### 3. 关键异常是否已有处理？

**代码层面已通过集成测试覆盖 30 项异常场景，但浏览器验证未完成。**

- T13-03 边界场景 16 种（12 用户端 + 4 管理端）已写为集成测试代码。状态为「进行中 — 浏览器验证待环境」。
- T13-02 端到端 9 条验证场景 + 追加 PRD 映射 + 追加 3 条，代码 30 项测试全部通过。
- MyReservations.cshtml 取消失败使用 toast 错误提示 ✅（非 `alert()`）

### 4. 状态显示是否统一？

**基本统一。**

- 用户端：已预约 = `badge bg-success`（绿色）/ 已取消 = `badge bg-secondary`（灰色） ✅
- 管理端座位：启用 = `badge bg-success` / 禁用 = `badge bg-danger`（红色） ✅
- 管理端预约：已预约 = 绿色 / 已取消 = 灰色 ✅
- 管理端登录状态：Session 检查 + AJAX 401 统一拦截 ✅
- 无矛盾或不一致的展示风格。

### 5. 空状态和提示信息是否补齐？

**基本补齐。**

| 页面 | 空状态 | 状态 |
|------|--------|------|
| Seats 列表（用户端） | 筛选无匹配时引导 | ✅（Sprint 1） |
| Detail 预约记录 | 无记录时提示 | ✅（Sprint 1） |
| MyReservations | else 分支显示引导链接 | ✅ |
| Admin/Seats | `@if (!Model.Any())` "暂无座位" | ✅（Sprnt 2） |
| Admin/Reservations | 无匹配"暂无匹配的预约记录" | ✅（Sprint 2） |
| Admin/Stats Rankings | `@if (!Model.SeatRankings.Any())` "暂无数据" | ✅ |
| Admin/Stats Trends | `@if (!Model.DailyTrends.Any())` else "暂无预约数据" | ✅ |

### 6. 用户端手机体验和管理端桌面体验是否有明显改进？

**无明显针对性改进。**

- 基于 Bootstrap 5 默认响应式断点，用户端页面（`col-md-*` 网格）在手机上自动堆叠。
- 管理端使用 `table-responsive` + `table-hover`，桌面体验良好。
- 但本轮无专门针对移动端或桌面端的 CSS 调整或布局评审记录。
- T13-01 未包含响应式相关的原型评审修复。

### 7. 如果使用了默认优先级或当前假设，是否已经在记录文档中说明？

**❌ 无法确认——docs/15 不存在。**

预留的文档记录文件 `docs/15-功能完善与体验优化记录.md` 未创建，因此没有任何关于优先级分配、假设条件、决策理由的记录。

### 8. 任务板是否真实记录了本轮任务卡、状态流转和完成统计？

**部分真实，但存在 3 处不真实记录。**

| 卡片 | 标记状态 | 实际完成度 | 偏差 |
|------|---------|-----------|------|
| T13-01 | **已完成** / ✓ 已通过 | ② invalid-feedback 未实现 | ❌ 未达验收标准却标完成 |
| T13-04 | **已完成** / ✓ 已通过 | 无输出文档记录不一致项 | ❌ 无审计产出验证 |
| T13-05 | **已完成** / ✓ 已通过 | ① XML 注释未添加；③ dotnet build 0 warnings ✅；② README 存在事实错误 | ❌ 未达验收标准却标完成 |

其余卡片状态真实：
- T13-02「进行中」浏览器验证待环境 ✅
- T13-03「进行中」浏览器验证待环境 ✅
- T13-06「待开发」需外网 ✅

任务数量统计（6 总 / 3 完成 / 50%）**单纯计数正确**，但 3 张已完成卡中至少 2 张**实质未达标**。

### 9. README 是否已经按本轮真实结果做了增量更新？

**有更新但存在 3 处事实错误。**

✅ **增量更新项：**
- Sprint 3 进度（3/6）及卡片状态 ✅
- 文档计数从 docs/01~13 更新为 docs/01~14 ✅
- 集成测试项数（30 + 20）✅

❌ **事实错误（T13-05 应修正但未修复）：**

| README 位置 | 错误内容 | 实际项目 | 引入来源 |
|-------------|---------|---------|---------|
| 第14行 | "net10.0" | net8.0 | 历史遗留（docs/07 审计 D02 已记录偏差但 README 未同步） |
| 第16行 | "Entity Framework Core 10.x" | EF Core 8.x | 同上 |
| 第16行、第155行、第158行 | "Code First + Migrations" / `db.Database.Migrate()` | `EnsureCreated()` | 同上 |

docs/11 §14 D01/D02 已记录这些事实偏差并标注"可接受，已有追溯"，但 README 作为**面向新用户的引导文档**未同步为真实状态，可能阻碍新用户一键启动。

### 10. 如果本轮尚未达到阶段最低完成线，结论是否仍然停留在"继续阶段15"？

**✅ 当前结论正确——任务板"进行中"列标注为"Sprint 3 进行中"，已完成 3/6。**

但存在隐患：**3 张已完成卡实际有未达标验收点**，若以此为基准判断"阶段最低完成线已达标"会得出错误结论。

Sprint 3 阶段最低完成线 3 条：
| 条件 | 是否达到 | 说明 |
|------|---------|------|
| 两条主链路在真实浏览器中完整走通无报错 | ❌ | 无浏览器环境，无法验证 |
| `dotnet build` 0 errors 0 warnings | ✅ | 持续保持 |
| README 可引导新用户成功启动 | ❌ | 事实错误可能阻碍一键启动 |

结论：**应继续阶段15（Sprint 3），不可进入下一步。**

### 11. Sprint 3 记录是否真实？

**任务板 Sprint 3 §2 记录部分真实。**

- 任务卡标题、验收标准、关联文档 ✅
- 完成卡标记与实际有偏差（见检查点8）
- Sprint 3 没有站会记录（§3~§14 只有 Sprint 1/2 的站会/评审/回顾记录）— Sprint 3 的记录尚未写入

**缺少 Sprint 3 评审记录**，这是合理的——Sprint 3 尚未完成，还不到评审时刻。

### 12. 是否已经具备进入下一步"联调、测试与缺陷闭环"的条件？

**❌ 不具备。**

| 条件 | 状态 |
|------|------|
| Sprint 3 阶段最低完成线 | ❌ 未达到 |
| 全部任务卡验证通过 | ❌ 6 张卡仅 3 张标记完成，其中 2 张实质未达标 |
| 浏览器端验证 | ❌ 无环境 |
| docs/15 存在 | ❌ |
| README 事实错误 | ❌ 未修正 |

进入下一步的前提是完成 Sprint 3 全部 6 张卡 + 阶段最低完成线达标。

### 13. 是否只把前序文档和前序记录当作缺口来源？

**✅ 是。** 本轮审计未扩大范围至阶段14或更早阶段的全量返工。发现的非阻塞历史问题记录如下：

| 历史问题 | 来源 | 是否阻塞本轮 | 处理建议 |
|---------|------|------------|---------|
| README net10.0 / Migrations | docs/11 D01/D02 | 不阻塞（已知偏差） | Sprint 3 内修复 |
| T13-02/03 浏览器验证 | 全局无浏览器环境 | 不阻塞（已标注） | 环境就绪后执行 |
| Sprint 1/2 站会记录完备但 Sprint 3 无 | 时间顺序 | 不阻塞（Sprint 未结束） | Sprint 3 完成后补写 |

---

## C. 发现的问题清单

### P0 级（阻塞进入下一步前必须解决）

| 编号 | 位置 | 问题描述 |
|------|------|---------|
| **P0-01** | `docs/15-功能完善与体验优化记录.md` | 文件不存在。阶段15的关键开发记录文档未创建 |

### P1 级（应在本阶段内解决）

| 编号 | 文件 | 问题描述 |
|------|------|---------|
| **P1-01** | `LibrarySeatSystem\Views\Home\Reserve.cshtml:29-52` | T13-01 验收标准②：表单校验未使用 Bootstrap `invalid-feedback` 样式。当前仅 HTML5 `required`，缺少 `was-validated` / `is-invalid` / `invalid-feedback` 组合 |
| **P1-02** | `LibrarySeatSystem\Services\*.cs` | T13-05 验收标准①：Service 接口方法无 XML 注释。全部 4 个接口中无任何 `/// <summary>` / `/// <param>` / `/// <returns>` 注释 |
| **P1-03** | `README.md:14,16,155,158` | T13-05 验收标准②：「README 更新为当前 Sprint 完成后的实际状态」未达标。README 第14行写 `net10.0`（实际 net8.0）、第16行 `EF Core 10.x`（实际 8.x）、第155行 `db.Database.Migrate()`（实际 `EnsureCreated()`） |
| **P1-04** | 任务板 Sprint 3 | T13-04 代码一致性审计无产出记录。无审计报告、无审计过程记录、无可验证的不一致项清单 |

### P2 级（建议优化，不阻塞）

| 编号 | 问题 | 说明 |
|------|------|------|
| R01 | 任务板 T13-01/T13-04/T13-05 标记为已完成但实际有未达标验收点 | 建议更新状态为「待修复」或注明未达标项 |
| R02 | Sprint 3 缺少站会/评审/回顾记录 | Sprint 3 尚未完成，但可提前记录里程碑决策 |
| R03 | MyReservations 取消操作仍使用 `confirm()` | 虽不属于 T13-01，但可考虑统一为 Bootstrap Modal 优化体验 |

---

## D. 必改项

| 编号 | 操作 | 依据 | 优先级 |
|------|------|------|--------|
| A01 | **创建 `docs/15-功能完善与体验优化记录.md`**，记录 Sprint 3 实现决策、验收点完成情况、未完成项、已知限制 | P0-01 | **阻塞级** |
| A02 | **修复 P1-01**：Reserve.cshtml 表单添加 `was-validated` 客户端验证 + `invalid-feedback` 样式 | P1-01 | 高 |
| A03 | **修复 P1-02**：4 个 Service 接口（ISeatService/IReservationService/IStatsService/IUserService）的公开方法补充 XML 注释 | P1-02 | 高 |
| A04 | **修复 P1-03**：README 中 net10.0 → net8.0、EF Core 10.x → 8.x、Migrations → EnsureCreated；确认 `dotnet run` 一键启动流程可被 README 正确引导 | P1-03 | 高 |
| A05 | **修复 P1-04**：补充 T13-04 审计产出——至少记录目录结构、实体字段、方法签名的一致性检查结论 | P1-04 | 高 |

---

## E. 建议优化项

| 编号 | 建议 |
|------|------|
| S01 | 任务板「进行中」列增加 Sprint 3 子状态列（代码就绪 / 浏览器验证待环境 / 阻塞），避免已通过集成测试的任务卡被误判为「进行中」 |
| S02 | 任务板 T13-01/T13-04/T13-05 改为「⚠ 部分达标」或添加备注标注未满足的验收点 |
| S03 | `confirm()` 弹窗可考虑升级为 Bootstrap Modal，统一全站交互体验 |

---

## F. 是否可以进入下一步"联调、测试与缺陷闭环"

**❌ 不可以。**

| 阻止因素 | 详情 |
|---------|------|
| P0 阻塞 | `docs/15-功能完善与体验优化记录.md` 不存在 |
| 阶段最低完成线未达标 | ① 浏览器验证未完成 ② README 存在事实错误可能阻碍新用户启动 ③ 3/6 卡未完成 |
| 已完成卡质量偏差 | 3 张已完成卡中至少 2 张有未达标的验收点（T13-01 ②、T13-05 ①/②） |

**建议：** 完成 A01~A05 全部必改项后，完成 T13-02/T13-03 浏览器端验证 → T13-06 推送 → 阶段最低完成线达标 → 再评估是否可进入下一步。
