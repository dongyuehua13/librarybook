# 《图书馆座位预约系统》开发前一致性总审计

> 审计范围：docs/01 ~ docs/10 全部文档 + 当前代码（Controllers/Models/Data/Program.cs）
> 审计方法：逐项核对交叉引用 + 文档 vs 代码比对

---

## A. 总体判断

全部 10 个阶段文档链一致，无结构性断裂。**发现 4 项必改项**（均为代码与文档不一致）、**4 项建议优化项**。总体可接受，修复后即可启动 Sprint 1。

---

## B. 发现的问题清单

### B1 数据模型层（代码 vs docs/08）

| 编号 | 问题 | 详情 | 严重度 |
|------|------|------|--------|
| C01 | **AppDbContext 缺少 Username 唯一索引** | docs/08 §4.4 指定 `HasIndex(u => u.Username).IsUnique()`，但 `AppDbContext.cs:14-25` 未实现 | **必改** |
| C02 | **AppDbContext 缺少 DeleteBehavior.NoAction** | docs/08 §4.4 指定 `OnDelete(DeleteBehavior.NoAction)`，但 `AppDbContext.cs:16-24` 使用默认值（Cascade） | **必改** |
| C03 | **Reservation.Date 类型不匹配** | docs/08 §4.3 指定 `date` 类型，但 `Reservation.cs:15` 使用 `DateTime`（EF Core 默认映射为 `datetime2`）。缺少 `[Column(TypeName = "date")]` | 建议优化 |
| C04 | **今日预约统计口径不一致** | docs/07 §7.1 和 docs/08 §8.1 均写为 `COUNT WHERE Date=today`（无 Status 过滤），但 docs/09 §2.4 写为 `CountAsync(r => r.Date == today && r.Status == "已预约")`（有过滤）。首页应展示有效预约数，以 docs/09 为准统一口径 | **必改** |

### B2 架构模式（代码 vs docs/07、docs/09）

| 编号 | 问题 | 详情 | 严重度 |
|------|------|------|--------|
| C05 | **缺少 Service 层 DI 注册** | docs/09 §4.3 规定 6 个 Service 接口/实现的 `AddScoped` 注册，但 `Program.cs:6-17` 无注册代码 | _参见备注①_ |
| C06 | **缺少 AdminAuthFilter** | docs/09 §3.3 规定 `[ServiceFilter(typeof(AdminAuthFilter))]`，当前 `AdminController.cs` 使用 `IsAdmin()` 手动判断 | _参见备注①_ |
| C07 | **控制器使用 `_db` 直连而非 Service** | docs/07 §4.2 禁止 Controller 直接读写 `AppDbContext`，当前两个 Controller 均直接 `_db.xxx` 调用 | _参见备注①_ |
| C08 | **使用 ViewBag 而非 ViewModel** | docs/07 §4.3 规定页面使用 ViewModel，当前 `HomeController.Index/Seats/Detail` 和 `AdminController` 全部使用 ViewBag | _参见备注①_ |

> 备注①：C05-C08 已在 README 和任务板 Sprint 0（T10-05 ~ T10-09）明确标注为待重构项。**不视为本次审计的缺陷**，但需在 Sprint 0 完成后消除差异。

### B3 业务规则（代码 vs docs/02、docs/03、docs/09）

| 编号 | 问题 | 详情 | 严重度 |
|------|------|------|--------|
| C09 | **代码有额外规则未记入文档** | `HomeController.Reserve.cs:89-91` 单次≤4h、`:95-101` 每日≤2次、`:113-121` 同用户同时段重复拦截 — 三条规则在全部 10 份文档中均未提及 | **必改**（选其一：回滚或更新 PRD/docs/03） |

### B4 管理端功能缺失（代码 vs docs/09）

| 编号 | 问题 | 详情 | 严重度 |
|------|------|------|--------|
| C10 | **SeatCreate 缺少查重校验** | docs/09 §3.4 规定 `CreateSeatAsync` 含座位编号查重，但 `AdminController.SeatCreate:62-68` 直接保存，无查重 | **必改** |
| C11 | **SeatEdit 更新方式不匹配** | docs/09 §3.4 规定字段逐一手动更新（`existing.SeatNumber = model.SeatNumber`），当前代码使用 `_db.Seats.Update(seat)` 全量更新 | 建议优化 |

### B5 文档间不一致

| 编号 | 问题 | 详情 | 严重度 |
|------|------|------|--------|
| C12 | **页面数描述不统一** | docs/02 §5.1 写"10 个页面"（含 SwitchUser），docs/03 §7 写"9 个页面"（SwitchUser 嵌入导航栏），两处计数口径不一致 | 建议优化 |
| C13 | **参数名不一致** | docs/09 §3.2 管理端预约筛选使用 `dateFrom/dateTo`，实际 `AdminController.Reservations.cs:101` 使用 `startDate/endDate` | 建议优化 |

---

## C. 必改项汇总

| 优先 | 编号 | 位置 | 要求 | 依据 |
|------|------|------|------|------|
| P0 | C01 | `AppDbContext.cs` | 添加 `HasIndex(u => u.Username).IsUnique()` | docs/08 §4.4 |
| P0 | C02 | `AppDbContext.cs` | 为两个 FK 配置添加 `.OnDelete(DeleteBehavior.NoAction)` | docs/08 §4.4 |
| P0 | C04 | `HomeController.cs:21`, docs/07 §7.1, docs/08 §8.1 | 今日预约数统一为 `Count(Status == "已预约")`；同步修正 docs/07 代码行和 docs/08 SQL | docs/09 §2.4（统一口径） |
| P0 | C10 | `AdminController.cs:62-68` | SeatCreate 添加 `AnyAsync(s => s.SeatNumber == model.SeatNumber)` 查重 | docs/09 §3.4 |
| P0 | C09 | 二选一 | **选项 A**：回滚代码中 3 条额外规则（≤4h、≤2次、同用户同时段）以对齐文档；**选项 B**：更新 docs/03 F4 的"最小验收标准"和 docs/09 §2.3 的 Controller 参数校验列表，将 3 条规则正式记入文档 | 文档代码一致性 |

---

## D. 建议优化项

| 编号 | 位置 | 建议 | 理由 |
|------|------|------|------|
| R01 | `Reservation.cs:15` | 加 `[Column(TypeName = "date")]` | 对齐 docs/08 §4.3 的 `date` 类型 |
| R02 | `AdminController.cs:101` | 参数名改为 `dateFrom/dateTo` | 对齐 docs/09 §3.2 |
| R03 | `AdminController.cs:79-86` | SeatEdit 改为逐字段更新 + 编辑时编号查重 | 对齐 docs/09 §3.4 |
| R04 | docs/01 §7 + docs/02 §5.1 | 页面数统一改为"9 页" | 消除文档间矛盾 |

---

## E. 是否可以进入 Sprint 1

**有条件可以。**

C01-C05 和 C10 五项 P0 必改项修复后方可进入 Sprint 1。C09（额外业务规则）需在"回滚代码"或"更新文档"之间二选一，建议采用 **选项 B（更新文档）** 以保留已有功能。

建议修复顺序：
1. C01 + C02 — AppDbContext 配置（约 5 分钟）
2. C04 + C05 — 统计口径修正 + 查重校验（约 10 分钟）
3. C09 选项 B — 更新 docs/03 和 docs/09（约 15 分钟）
4. Sprint 1 启动

> 审计日期：2026-07-07 · 审计范围：10 份文档 + 全部现有代码

---

## F. 修复结果（审计后执行）

| 编号 | 修复内容 | 状态 | 文件 |
|------|---------|------|------|
| C01 | 添加 `HasIndex(u => u.Username).IsUnique()` | ✅ 已修复 | `Data/AppDbContext.cs:23-24` |
| C02 | 两个 FK 添加 `.OnDelete(DeleteBehavior.NoAction)` | ✅ 已修复 | `Data/AppDbContext.cs:17,22` |
| C04 | 今日预约数改为 `Count(r => r.Date == today && r.Status == "已预约")`；同步更新 docs/07 §7.1 和 docs/08 §8.1 | ✅ 已修复 | `Controllers/HomeController.cs:21`, `docs/07-系统设计说明.md:274`, `docs/08-数据库设计.md:261` |
| C09 选项 B | docs/03 F4 增补 3 条规则到业务规则和验收标准；docs/09 §2.3 增补 ≥4h 校验、每日上限、同用户冲突到 Service 和异常表 | ✅ 已修复 | `docs/03-PRD-Lite.md` §F4, `docs/09-关键链路详细设计.md` §2.3/§2.4/§2.8 |
| C10 | SeatCreate 添加 `Any(s => s.SeatNumber == model.SeatNumber)` 查重 | ✅ 已修复 | `Controllers/AdminController.cs:68-72` |
| R01 | `Reservation.Date` 加 `[Column(TypeName = "date")]` | ✅ 已修复 | `Models/Reservation.cs:16` |
| R02 | AdminController.Reservations 参数名改为 `dateFrom/dateTo`，View 同步更新 | ✅ 已修复 | `Controllers/AdminController.cs:101`, `Views/Admin/Reservations.cshtml:22,26` |
| R03 | SeatEdit 改为逐字段更新 + 编辑时编号查重 | ✅ 已修复 | `Controllers/AdminController.cs:83-97` |
| R04 | docs/01 §7 + docs/02 §5.1 页面数统一为"9 页" | ✅ 已修复 | `docs/01-项目立项单.md:78,117`, `docs/02-需求分析与MVP确认.md:152` |
| — | `dotnet build` 验证 | ✅ 0 errors 0 warnings | — |

**结论：全部必改项和建议项已修复。可以启动 Sprint 1。**
