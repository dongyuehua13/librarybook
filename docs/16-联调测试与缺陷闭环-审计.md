# 《图书馆座位预约系统》联调、测试与缺陷闭环 — 审计报告

> 审计日期：2026-07-13  
> 审计对象：README.md、docs/16-联调测试与缺陷闭环.md、docs/项目任务板与迭代记录.md、src/  
> 对照基线：docs/03/06/07/08/09/11/13/14/15、任务板 Sprint 3  
> 审计范围：仅审阶段16新增结果（Sprint 4），不重做阶段15或更早阶段的全量审计

---

## A. 总体判断

**不通过。阶段16（Sprint 4）尚未启动。** 项目当前仍停留在阶段15（Sprint 3）的收尾状态（5/6 = 83%），Sprint 3 的允许进入下一阶段条件"全部任务卡验证通过 → 项目交付"**未达到**（T13-02/T13-03 浏览器验证阻塞）。

阶段16要求的全部审计对象均不存在。

---

## B. 21 项检查点逐项结果

### 1. 联调场景、测试场景和验收重点是否优先来自成功标准、任务板、前序审计结论和前序开发记录？

**❌ 无法评估——阶段16未启动。** 无 docs/16 记录文件，无 Sprint 4 任务卡，无测试场景清单。前序审计（docs/15 审计 §D A02/A03/A04）留下的 README 事实错误、XML 注释缺失、T13-04 产出缺失等 P1 项尚未修复。

### 2. 用户端和管理端是否已经真正联通？

**代码层面已联通。** 两套 Controller（HomeController + AdminController）共享同一 DbContext，管理端的座位启用/禁用直接影响用户端的 `GetSeatsWithStatusAsync` 过滤。但**浏览器端从未验证**，真正的 HTTP 联调未执行。

### 3. 主流程是否可以完整演示？

**代码层面可以。** README §建议演示路径 列出了完整的 18 步用户端+管理端演示路径。但从未在真实浏览器中实际执行过。无 Playwright 自动化录屏或人工验证记录佐证。

### 4. 关键规则是否生效？

**后端规则已验证。** 30 项集成测试验证了全部业务规则：
- ✅ 冲突检测（同座位同日期同时段不可重复预约）
- ✅ 跨日期不冲突
- ✅ 相邻时段不冲突
- ✅ 取消后重新预约
- ✅ 不可取消过期/已开始
- ✅ 禁用不可约
- ✅ 禁用前台隐藏

**前端规则未验证。** 表单校验（invalid-feedback）仅在代码层面，无浏览器端验证记录。

### 5. 是否把 `Node.js` 视为第一阶段已完成前提？

**❌ 不适用的正向表述。** 阶段16尚未启动，不存在 Node.js 安装记录。README 运行前提中未列出 Node.js 依赖（这是正确的，当前阶段不需要 Node.js）。

### 6. 是否显式安装或复用了 `@playwright/test`，并补齐了 `playwright.config.*`？

**❌ 不存在。** `package.json`、`playwright.config.*`、`node_modules/` 均不存在。无 Playwright 依赖。

### 7. 默认自动化配置是否明确使用了 `channel: 'msedge'`？

**❌ 无配置。** 无任何 Playwright 配置文件。

### 8. 是否显式执行了基于 `msedge` 的 Playwright 自动点击？

**❌ 未执行。** T13-02/T13-03 的浏览器验证状态均为"待浏览器环境"。无 Playwright 脚本，无自动化点击记录。

### 9. 是否显式做了脚本烟雾测试？

**❌ 未执行。** 无 `scripts/` 目录，无 smoke 测试脚本。

### 10. 是否显式做了兼容性测试？

**❌ 未执行。** 无跨浏览器测试记录。

### 11. 是否显式做了回归测试？

**部分完成（属于阶段15）。** `dotnet test` 30/30 全部通过 ✅。但这是 xUnit 后端集成测试，属于阶段15 Sprint 3 的 T13-02/T13-03 产出，不是阶段16新增的回归测试。无新增回归测试用例。

### 12. 是否形成了 Bug 闭环？

**❌ 无 Bug 跟踪。** 项目无 Bug 清单，无缺陷卡，无修复-验证-关闭流程。前序审计发现的问题（P1-01 ~ P1-04 from phase 15）均未被跟踪修复。

### 13. 如果用了默认补足测试项或当前假设，是否已经显式写出？

**❌ 无 docs/16，无法评估。**

### 14. 如果远端仓库连接不稳定，是否至少完成了本地 commit？

**✅ 本地 commit 已完成。** 最新 commit `80e5300`（Sprint 3 R2）在本地 `dev` 分支。且已标记 T13-06 推送完成（commit `d4ca931` 已推送到 origin/dev）。

### 15. 任务板是否真实记录了本轮测试项、缺陷卡、状态流转和完成统计？

**❌ 阶段16未创建。** 任务板截至 Sprint 3（§2），无 Sprint 4 视图。Sprint 3 仍为 5/6 83%，T13-02/T13-03 浏览器验证待环境。无缺陷卡。

### 16. README 是否已按本轮真实联调结果做了增量更新？

**❌ 未更新。** README 仍为 Sprint 3 口径（§已实现范围 第89行 "当前为 Sprint 1 完成、Sprint 2 进行中（4/8）、Sprint 3 部分完成状态"——这句描述本身已过时，Sprint 2 实际为 8/8 完成）。README 仍有遗留事实错误：

| 位置 | 错误内容 | 实际 |
|------|---------|------|
| 第14行 | `net10.0` | `net8.0` |
| 第16行 | `EF Core 10.x (Code First + Migrations)` | `EF Core 8.x (EnsureCreated)` |
| 第158行 | `db.Database.Migrate()` | `db.Database.EnsureCreated()` |

这些问题自 docs/15 审计即被标记为 P1-03，至今未修复。

### 17. 如果本轮尚未达到阶段最低完成线，或 P0 仍未清零，结论是否仍然停留在"继续阶段16"？

**✅ 当前停留在正确阶段（Sprint 3 进行中）。** 任务板显示 Sprint 3 83%，"进行中"列包含 T13-02/T13-03，未误判可进入下一阶段。

但存在隐患：README 第216行写 `**Sprint 3 — 集成与完善**（5/6 ✅ · 代码层面完整，浏览器验证待环境）`，如果将其视为"Sprint 3 已完成"的表述会导致误判。实际上 5/6 意味着未完成，应理解为"进行中"而非"已完成"。

### 18. P0 / P1 验收口径是否清楚，P0 是否已关闭？

**无 P0 阻碍。** 当前无 P0 阻塞项在任务板中登记。但**docs/15 审计发现的 P1 问题均未关闭**：

| 编号 | 问题 | 状态 |
|------|------|------|
| P1-01 | Reserve.cshtml invalid-feedback（已修复 ✅） | ✅ |
| P1-02 | Service 接口 XML 注释缺失 | ❌ 未修复 |
| P1-03 | README 事实错误（net10.0/Migrations） | ❌ 未修复 |
| P1-04 | T13-04 代码一致性审计无产出 | ❌ 未修复 |
| P1-05 | 阶段16 全量缺失（本期新发现） | N/A |

### 19. Sprint 4 记录是否真实？

**❌ Sprint 4 不存在。** 任务板无 Sprint 4 定义，无任务卡，无站会/评审/回顾记录。

### 20. 是否可以进入最终交付？

**❌ 不可以。** 主要原因：

| 条件 | 状态 | 说明 |
|------|------|------|
| Sprint 3 全部任务完成 | ❌ | 5/6，T13-02/03 浏览器验证未完成 |
| Sprint 3 阶段最低完成线 | ❌ | 浏览器验证未完成 |
| Sprint 4（联调测试缺陷闭环） | ❌ | 未启动 |
| README 事实错误 | ❌ | net10.0/Migrations 未修正 |
| 浏览器端验证 | ❌ | 无环境 |
| Bug 闭环 | ❌ | 无跟踪 |

### 21. 是否只把前序文档和前序记录当作联调基线？

**✅ 是。** 本轮审计未扩大范围至阶段15或更早阶段的全量返工。历史遗留问题记录如下：

| 非阻塞历史问题 | 来源 | 备注 |
|--------------|------|------|
| README net10.0 / Migrations | docs/11 D01/D02 | 自 Sprint 0 即存在，docs/15 审计列为 P1-03 |
| Service 无 XML 注释 | docs/15 审计 P1-02 | 从未修复 |
| T13-04 无产出记录 | docs/15 审计 P1-04 | 从未修复 |
| 浏览器端无法验证 | 全局无环境 | 贯穿 Sprint 1~3 |

---

## C. 发现的问题清单

### P0 级（阻塞进入阶段16）

| 编号 | 问题 | 说明 |
|------|------|------|
| **P0-01** | Sprint 3 未完成（5/6） | T13-02/T13-03 浏览器验证待环境。进入阶段16的前提是完成 Sprint 3 |
| **P0-02** | `docs/16-联调测试与缺陷闭环.md` 不存在 | 阶段16的关键记录文档未创建 |
| **P0-03** | `package.json` + `playwright.config.*` 均不存在 | 阶段16核心工具链未搭建 |
| **P0-04** | 任务板无 Sprint 4 定义 | 无任务卡、无验收标准、无测试项 |

### P1 级（应在阶段16过程中解决）

| 编号 | 问题 | 严重性 |
|------|------|--------|
| P1-01 | README 第14行 `net10.0` 应为 `net8.0` | 阻止新用户一键启动 |
| P1-02 | README 第16行 `EF Core 10.x (Code First + Migrations)` 与事实不符 | 同上 |
| P1-03 | Service 接口无 XML 注释（4 个接口） | 代码可维护性 |
| P1-04 | T13-04 代码一致性审计无产出记录 | 审计闭环缺失 |

---

## D. 必改项

| 编号 | 操作 | 依据 | 优先级 |
|------|------|------|--------|
| A01 | **完成 Sprint 3 收尾**：明确 T13-02/T13-03 浏览器验证的替代方案（如人工验证 checklist 或本地环境验证） | P0-01 | **阻塞级** |
| A02 | **修复 README 事实错误**：net10.0 → net8.0，Migrations → EnsureCreated | P1-01/P1-02 | 高 |
| A03 | **创建 Sprint 4 任务板定义**：包含联调、Playwright 搭建、e2e 场景、烟雾测试、兼容性测试、Bug 闭环等卡片 | P0-04 | 高 |

**阶段16 启动前必须：** Sprint 3 全部完成 → 阶段最低完成线达标 → 再启动 Sprint 4。

---

## E. 建议优化项

| 编号 | 建议 |
|------|------|
| S01 | Sprint 3 的 T13-02/03 浏览器验证可在 Sprint 4 的 Playwright 环境搭建完毕后统一执行，不必单独等待浏览器环境 |
| S02 | README 第89行的陈旧过渡描述"当前为 Sprint 1 完成、Sprint 2 进行中（4/8）"应及时更新为实际状态（Sprint 3 5/6） |
| S03 | 新增 Sprint 4 时建议采用最小增量：先搭 Playwright + 1 条 smoke 测试（验证首页 200），再逐步扩充 e2e 场景 |
| S04 | 建议将 Service 接口 XML 注释纳入 Sprint 4 的"代码质量"卡片统一处理 |

---

## F. 是否可以进入下一步"交付、仓库提交与复盘"

**❌ 不可以。**

| 阻止因素 | 状态 |
|---------|------|
| Sprint 3 未完成（5/6） | ❌ |
| Sprint 3 阶段最低完成线未达标 | ❌ |
| Sprint 4（联调测试缺陷闭环）未启动 | ❌ |
| README 事实错误未修复 | ❌ |
| 浏览器端验证从未执行 | ❌ |
| 无 Bug 跟踪闭环 | ❌ |

**当前阶段：应继续 Sprint 3 收尾 + 启动 Sprint 4。** 项目从 Sprint 1 到 Sprint 3 的代码交付质量高（build ✅、test ✅、功能完整），但缺乏浏览器端验证和自动化测试。建议在完成 Sprint 3 收尾和 README 修正后，以最小代价启动 Sprint 4（Playwright + 1 smoke test），达到全部 6 条 Sprint 的完成条件后方可进入最终交付。
