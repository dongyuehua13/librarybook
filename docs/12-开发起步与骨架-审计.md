# 《图书馆座位预约系统》开发起步与项目骨架 — 审计报告

> 审计日期：2026-07-13  
> 审计对象：README.md、.sln、LibrarySeatSystem/、src/LibrarySeatReservation.Web/、docs/11、docs/12、任务板  
> 审计方法：16 项检查点逐项核对，对照 docs/07/08/09/11 作为门禁依据

---

## A. 总体判断

**有条件通过。** 项目的实际可运行代码（`LibrarySeatSystem/`）已完整跨越 Sprint 0/1/2 三个阶段，Sprint 3 完成 3/6。但仓库存在**两套并行的项目结构**，`.sln` 指向了未完成的项目 `src/LibrarySeatReservation.Web/`，而 docs/12 也记录的是那套项目。导致文档、`.sln` 和实际代码之间出现结构性矛盾。

**判定：** 只要解决仓库根目录的双项目结构问题（清理或统一 `.sln` 指向），即可进入下一步开发。

---

## B. 16 项检查点逐项结果

### 1. 是否先通过了 docs/11 的 Go / No-Go 门禁？

docs/11 §12 结论 "✅ Go — 可以进入开发"。P0 = 0，P1 共 4 项：
- P1-01/P1-02 → 已在 `LibrarySeatSystem/` 中修复（IUserService+UserService 已存在，ValidateAdminAsync 已移入）
- P1-03/P1-04 → 任务板 T13-02 验收标准已补充

**但注意：** `src/LibrarySeatReservation.Web/` 的实现状态不详，其 P1-01/P1-02 是否修复未验证。

### 2. 工程结构是否清楚？

**对于 `LibrarySeatSystem/`：✅ 清楚。** 全部目录（Controllers/Models/ViewModels/Services/Filters/Data/Views/wwwroot）均存在且包含内容。
**对于仓库整体：❌ 不清楚。** 存在两套独立项目：

| 项目 | 位置 | .sln 包含？ | 完成度 | 技术栈 |
|------|------|-----------|--------|--------|
| 主项目 | `LibrarySeatSystem/` | ❌ 不在 .sln 中 | Sprint 0/1/2 ✅ | .NET 8, EF Core 8, EnsureCreated |
| 旧项目 | `src/LibrarySeatReservation.Web/` | ✅ 在 .sln 中 | 有骨架但未验证完整性 | .NET 10, EF Core 10, Migrations |

**致命问题：** `dotnet build` 在解决方案级别（`LibrarySeatReservation.sln`）构建的是旧项目，而非主项目。

### 3. 是否接入 EF Core 和 SQL Server LocalDB？

**`LibrarySeatSystem/`：✅ 已接入。** `Program.cs:16-17` `AddDbContext<AppDbContext>` 指向 `appsettings.json` 中 `(localdb)\MSSQLLocalDB;Database=LibrarySeatDb`。

**`src/LibrarySeatReservation.Web/`：✅ 也已接入。** 连接串指向 `Database=LibrarySeatReservation`。

### 4. Program.cs 注册是否合理？

**`LibrarySeatSystem/`：✅ 全部合理。** 包含：
- `AddControllersWithViews()` ✅
- `AddDbContext<AppDbContext>` ✅
- `AddScoped<ISeatService, SeatService>()` 等 4 个 Service ✅
- `AddScoped<AdminAuthFilter>()` ✅
- `AddDistributedMemoryCache()` + `AddSession()` ✅
- `app.UseSession()` 在 `UseRouting()` 和 `UseAuthorization()` 之间 ✅

### 5. DbContext、Entity、Service、DataAccess 是否已有清楚骨架？

**`LibrarySeatSystem/`：✅ 全部完整。**
- Entity：3 实体（User/Seat/Reservation）对齐 docs/08 §4
- DbContext：AppDbContext + 3 DbSet + FK NoAction + Username 唯一索引
- Service：4 接口 + 4 实现（IUserService, ISeatService, IReservationService, IStatsService）
- ViewModel：7 个 ViewModel 类
- Filter：AdminAuthFilter（IAuthorizationFilter 实现）
- DataAccess：Service 封装，Controller 不直调 DbContext

### 6. 是否已经创建首次迁移，并完成首次建库建表？

**`LibrarySeatSystem/`：✅ 无需迁移。** 使用 `EnsureCreated()` + `DbInitializer.Seed()` 自动建库。此策略在 README 中明确声明，与 docs/10 的开发策略一致。

**`src/LibrarySeatReservation.Web/`：⚠️ 已有迁移。** Migrations/ 目录包含 `InitialCreate` 迁移文件，但未验证是否已 apply。

### 7. 是否已经具备最小演示种子数据？

**`LibrarySeatSystem/`：✅ 具备。** 4 用户（admin + zhangsan/lisi/wangwu）+ 15 座位（A-01~C-05）。`if (db.Users.Any()) return;` 防重复。

**`src/LibrarySeatReservation.Web/`：⚠️ 种子数据规格不同。** 45 个座位（3层×3区域×5），与 docs/08 的 15 个设计值不一致。

### 8. 种子数据初始化方式是否写清楚，且和最终交付口径不冲突？

**README 和 `LibrarySeatSystem/`：✅ 一致。** "Code First + EnsureCreated()"策略、Seed 流程在 README 和 Program.cs 中完全对齐。

### 9. 命名空间、目录职责、实体命名、入口页命名是否与 docs/07/08/09 保持一致？

**`LibrarySeatSystem/`：✅ 一致。**
- 项目名 `LibrarySeatSystem` → docs/07 §3 ✅
- 实体 `User/Seat/Reservation` → docs/08 ✅
- 路由 `/Home/Seats` `/Home/Detail/{id}` `/Admin/Login` 等 → docs/09 §2.2 ✅
- 命名空间 `LibrarySeatSystem.*` → docs/07 §3 ✅

**`src/LibrarySeatReservation.Web/`：❌ 不一致。** 命名空间为 `LibrarySeatReservation.Web.*`，与 docs/07 约定的 `LibrarySeatSystem.*` 不符。此问题在 docs/11 §14 D03 中已记录为偏差。

### 10. 首页、用户端入口、管理端入口是否可访问？

**`LibrarySeatSystem/`：✅ 从代码可确认可访问。**
- `HomeController.Index` → `GET /` → Index.cshtml ✅
- `HomeController.SwitchUser` → `GET/POST /Home/SwitchUser` ✅
- `AdminController.Login` → `GET/POST /Admin/Login` ✅
- `AdminController.Seats` → `GET /Admin/Seats`（受 AdminAuthFilter 保护）✅

**未做浏览器级验证**（CLI 环境无浏览器），但此前 Session 中已确认浏览器操作全部可用。

### 11. 默认补足项或当前假设是否已显式写出？

**README：✅ 已写出。** "已知限制"列出了密码、注册、跨平台、定时任务、分页等边界。但缺少"为什么存在两个项目结构"的说明。

**双项目共存假设未说明：❌ 缺失。** 仓库根目录下 `LibrarySeatSystem/` 和 `src/LibrarySeatReservation.Web/` 同时存在且内容不同，没有任何文档解释二者的关系（谁为主、谁为历史遗留、如何清理）。

### 12. 任务板和 Sprint 0 状态是否已更新？

**整体：⚠️ 混合状态。**

| Sprint | docs/12 记录 | 任务板标注 | 实际代码状态 |
|--------|-------------|-----------|-------------|
| Sprint 0 | 100% (12/12) ✅ | 100% (12/12) ✅ | 全部完成 ✅ |
| Sprint 1 | 不涉及 | 100% (8/8) ✅ | 全部完成 ✅ |
| Sprint 2 | 不涉及 | 100% (8/8) ✅ | 全部完成 ✅ |
| Sprint 3 | 不涉及 | 50% (3/6) | T13-04/05 ✅, T13-02/03 测试代码存在 |

Sprint 0 状态在 docs/12 和任务板中标记为"已完成"，与实际代码一致。✅

**但注意：** 任务板中 T13-02/T13-03 仍标注"☐ 待开发"，但实际已生成测试代码（30 个集成测试）。状态需要同步。

### 13. 开发前总审计中的 P0 阻塞项是否确实已清零或已闭环？

**`LibrarySeatSystem/`：✅ 已清零。** P0 无。P1-01/P1-02 已修复并复核通过。P1-03/P1-04 验收标准已补充至任务板 T13-02。

### 14. README 是否已按本轮真实结果增量更新？

**✅ 准确且完整。** README 当前状态：
- 项目简介 + 技术栈 ✅
- 目录结构（✓/◇ 标注准确）✅
- 页面清单（含状态标注）✅
- 已实现范围 ✅
- 运行步骤 ✅
- 数据库初始化方式 ✅
- 演示账号 ✅
- 已知限制 ✅
- 当前阶段（Sprint 0 ✅ → Sprint 1/2/3 待完成）✅

**但缺少：** README 未提及存在两个项目结构的问题，也未说明 `.sln` 当前只关联旧项目。

### 15. 是否已经达到"能编译、能启动、能看到入口页、能完成首次建库建表和种子初始化"的目标？

**`LibrarySeatSystem/`：✅ 全部达标。** `dotnet build` 0 errors 0 warnings，`dotnet run` 可启动，`EnsureCreated` + `Seed` 自动完成建库建表+种子写入。

**仓库级：❌ 未达标。** `.sln` 指向旧项目，从解决方案级 `dotnet build` 会构建 `src/LibrarySeatReservation.Web/` 而非主项目。虽然 README 的"快速启动"步骤直接 `cd LibrarySeatSystem` 是正确的，但 `.sln` 文件的存在会误导 VS/IDE 用户在打开 .sln 时看到错误的项目。

### 16. 是否只围绕阶段12的放行条件审计？

**✅ 是的。** 所有发现的问题均为当前阶段产物的状态（双项目结构、`.sln` 指向、docs/12 记录偏差），未扩大至对前序文档的全量重审。docs/07/08/09/11 仅作为对照依据使用。

---

## C. 发现的问题清单

### P0 级（阻塞进入下一步前必须解决）

| 编号 | 文件 | 问题描述 |
|------|------|---------|
| **P0-01** | `LibrarySeatReservation.sln` | `.sln` 仅包含 `src/LibrarySeatReservation.Web/`，未包含主项目 `LibrarySeatSystem/` 和测试项目 `LibrarySeatSystem.Tests/`。从解决方案构建得到的是旧项目而非主项目 |
| **P0-02** | `docs/12-开发起步与骨架记录.md` | 全部文档指向 `src/LibrarySeatReservation.Web/`（Migrations, .NET 10, 45 座位），与主项目 `LibrarySeatSystem/`（EnsureCreated, .NET 8, 15 座位）的记录完全不一致 |
| **P0-03** | 仓库根目录 | 同时存在 `LibrarySeatSystem/`（主项目，完整）与 `src/LibrarySeatReservation.Web/`（旧项目，骨架态），无任何文档说明二者的关系，首次接触仓库者无法判断谁是主项目 |

### P1 级（应在本轮或 Sprint 3 内解决）

| 编号 | 文件 | 问题描述 |
|------|------|---------|
| **P1-01** | `任务板` | T13-02/T13-03 仍标注"☐ 待开发"，但实际已生成 30 个集成测试代码（`LibrarySeatSystem.Tests/`），应更新状态 |
| **P1-02** | `docs/12-开发起步与骨架记录.md` §7 | 种子数据记录为 45 个座位，与主项目实际的 15 个不一致 |
| **P1-03** | `docs/12-开发起步与骨架记录.md` §5 | 技术栈记录为 .NET 10 / EF Core 10.0.9，与主项目实际的 .NET 8 / EF Core 8.0.11 不一致 |
| **P1-04** | `docs/12-开发起步与骨架记录.md` §6 | 数据库初始化方式记录为"EF Core Migrations"，与主项目实际的 `EnsureCreated()` 不一致 |

---

## D. 必改项（进入下一步前必须修复）

| 编号 | 操作 | 理由 |
|------|------|------|
| A01 | **清理双项目结构：** 确定保留 `LibrarySeatSystem/` 为主项目，删除或归档 `src/LibrarySeatReservation.Web/`（或反向决策，统一后清理另一份） | P0-01/02/03 的根本性解决方案 |
| A02 | **更新 `.sln`：** 将 `LibrarySeatSystem/` 和 `LibrarySeatSystem.Tests/` 加入解决方案，移除旧项目引用 | P0-01 的直接修复 |
| A03 | **重写 docs/12：** 使 docs/12 的记录与 `LibrarySeatSystem/` 的实际技术栈、种子数据、建库方式保持一致 | P0-02 的直接修复 |

---

## E. 建议优化项

| 编号 | 建议 | 理由 |
|------|------|------|
| R01 | 任务板同步：将 T13-02/T13-03 标注从"☐ 待开发"更新为"测试代码已完成，待浏览器验证"或类似状态 | 反映实际进展 |
| R02 | README 增加"仓库结构说明"段落，解释双项目历史及清理计划 | 消除首次接触者的困惑 |
| R03 | docs/12 补充 Sprint 1/2 完成状态 | 当前 docs/12 仅覆盖 Sprint 0，后续 Sprint 无对应记录 |
| R04 | 删除或清理 `src/LibrarySeatReservation.Web/Migrations/` 中的迁移文件（如决定保留旧项目） | 避免残留的迁移文件与 `EnsureCreated` 策略混淆 |

---

## F. 是否可以进入下一步"用户端主链路开发"

### 对于 `LibrarySeatSystem/` 项目

**✅ 已可进入。** 实际上 Sprint 1（用户端主链路）在 `LibrarySeatSystem/` 中已经开发完成（8/8 ✅），Sprint 2 也已开发完成（8/8 ✅）。当前实际处于 **Sprint 3 收尾阶段**（3/6 ✅）。

### 对于仓库整体

**⚠️ 有条件通过。** 在完成以下 3 项清理后即可不受阻碍地继续：
1. **A01** — 清理双项目结构（二选一）
2. **A02** — 更新 `.sln`
3. **A03** — 重写 docs/12

### 推荐路线

```
步骤 1: 清理双项目结构（A01）
   ↳ 确认保留 LibrarySeatSystem/，移除或隔离 src/LibrarySeatReservation.Web/
步骤 2: 更新 .sln（A02）
   ↳ 将 LibrarySeatSystem/ + LibrarySeatSystem.Tests/ 加入解决方案
步骤 3: 重写 docs/12（A03）
   ↳ 使其记录与实际 LibrarySeatSystem/ 一致（.NET 8, EnsureCreated, 15 seats）
步骤 4: 同步任务板 + README（R01/R02）
步骤 5: 继续 Sprint 3（T13-04/05 已完，T13-06 待外网）
```
