# 《图书馆座位预约系统》开发起步与项目骨架 — 审计报告

> 审计对象：README.md、.csproj、Program.cs、Controllers/*.cs、Models/*.cs、Data/*.cs、docs/11-开发前一致性总审计.md、docs/项目任务板与迭代记录.md  
> 审计方法：16 项检查点逐项核对

---

## A. 总体判断

**未通过。** Sprint 0 尚未执行，工程骨架阶段处于"待开工"状态。当前仅完成了 Sprint 0 前置的一致性修复（已记入任务板"已完成"区），但 Sprint 0 的 9 张核心任务卡（T10-01 ~ T10-09）全部仍为"☐ 待开发"。`dotnet build` 可通过且数据库可自动初始化，项目具备启动 Sprint 0 的潜在条件，但"开发起步与项目骨架"阶段本身未完成交付。

---

## B. 16 项检查点逐项结果

### 1. 是否先通过了 docs/11 的 Go / No-Go 门禁？

✅ **通过。** `docs/11-开发前一致性总审计.md` §12 明确给出"✅ Go — 可以进入开发"，无 P0 阻塞项。门禁已开。

### 2. 工程结构是否清楚？

⚠️ **部分清楚，但骨架层尚未创建。**

| 目录 | 当前状态 | docs/07 §3 期望状态 | 差距 |
|------|---------|-------------------|------|
| Controllers/ | ✅ 存在 | ✅ 存在 | 无 |
| Models/ | ✅ 存在 | ✅ 存在 | 无 |
| Data/ | ✅ 存在 | ✅ 存在 | 无 |
| Views/ | ✅ 存在 | ✅ 存在 | 无 |
| ViewModels/ | ❌ 不存在 | ◇ Sprint 0 新建 | T10-06 未执行 |
| Services/ | ❌ 不存在 | ◇ Sprint 0 新建 | T10-05 未执行 |
| Filters/ | ❌ 不存在 | ◇ Sprint 0 新建 | T10-07 未执行 |
| .sln | ❌ 不存在 | ◇ 可选（单项目可不建） | 任务板标注"按需" |
| Migrations/ | ❌ 不存在 | 未要求（使用 EnsureCreated） | 非必须 |

README 目录结构中的 ✓/◇ 标注与实际状态一致。✅

### 3. 是否接入 EF Core 和 SQL Server LocalDB？

✅ **已接入。** 见 `Program.cs:8-9`：
```csharp
builder.Services.AddDbContext<AppDbContext>(options =>
    options.UseSqlServer(builder.Configuration.GetConnectionString("DefaultConnection")));
```
`appsettings.json:10` 指向 `(localdb)\MSSQLLocalDB;Database=LibrarySeatDb`。

### 4. Program.cs 注册是否合理？

⚠️ **部分合理，缺少 Service + Filter DI 注册。**

| 注册项 | 状态 | 说明 |
|--------|------|------|
| `AddControllersWithViews()` | ✅ | 标准 MVC 注册 |
| `AddDbContext<AppDbContext>` | ✅ | EF Core 正常 |
| `AddDistributedMemoryCache()` | ✅ | Session 前置 |
| `AddSession(...)` | ✅ | 4h 超时 |
| `app.UseSession()` | ✅ | 中间件管道路径正确 |
| `AddScoped<ISeatService, SeatService>()` | ❌ 缺失 | Sprint 0 T10-05 |
| `AddScoped<IReservationService, ReservationService>()` | ❌ 缺失 | Sprint 0 T10-05 |
| `AddScoped<IStatsService, StatsService>()` | ❌ 缺失 | Sprint 0 T10-05 |
| `AddScoped<AdminAuthFilter>()` | ❌ 缺失 | Sprint 0 T10-07 |

缺失项全部已在任务板中规划，不视为本次审计的缺陷，但需在 Sprint 0 完成后消除。

### 5. DbContext、Entity、Service、DataAccess 是否已有清楚骨架？

⚠️ **部分骨架存在：**

| 层 | 状态 | 详情 |
|----|------|------|
| Entity | ✅ 完整 | 3 实体（User/Seat/Reservation），字段对齐 docs/08 §4 |
| DbContext | ✅ 完整 | AppDbContext，含 3 DbSet + FK 配置 + 唯一索引 + NoAction |
| DataAccess | ⚠️ 存在但为直调模式 | 控制器直接 `_db.xxx`，无 Service 中介 |
| Service | ❌ 不存在 | T10-05 未执行 |
| ViewModel | ❌ 不存在 | T10-06 未执行 |
| Filter | ❌ 不存在 | T10-07 未执行 |

### 6. 是否已经创建首次迁移，并完成首次建库建表？

❌ **未使用 EF Core 迁移。** 当前策略为 `EnsureCreated()` + `DbInitializer.Seed()`（`Program.cs:37-41`）。此方案已在 README 中明确说明是**有意选择**的设计决策：

> 本项目不使用 EF Core 迁移（Migration）模式。`EnsureCreated()` 在开发阶段够用，但不会跟踪表结构变更。如果实体类字段发生变化，需手动删除数据库文件后重新运行。

对于课堂项目，此方案是可接受的。但需注意：
- 数据库需实际运行一次 `dotnet run` 才创建（当前未验证是否已实际创建）
- `EnsureCreated()` 不支持表结构变更跟踪，后续 Sprint 中如修改实体，需手动删库重建

### 7. 是否已经具备最小演示种子数据？

✅ **具备。** `Data/DbInitializer.cs` 包含：
- 4 个用户：admin（管理员）+ zhangsan/lisi/wangwu（学生）
- 15 个座位：A-01~A-05(1F 自习区) / B-01~B-05(2F 安静区) / C-01~C-05(3F 电子阅览区)，含 Description
- `if (db.Users.Any()) return;` 防重复写入

数据量与 docs 一致，可支撑切换账号、管理员登录（admin）和首页统计数据展示。

### 8. 种子数据初始化方式是否写清楚，且和最终交付口径不冲突？

✅ **写清楚且一致。** README §"数据库初始化方式"明确说明 `EnsureCreated()` + `Seed()` 流程，并注明"本项目不使用 EF Core 迁移"。与 docs/10 的开发策略声明一致。

### 9. 命名空间、目录职责、实体命名、入口页命名是否与 docs/07、docs/08、docs/09 保持一致？

✅ **一致。**

| 项目 | 代码 | docs/07/08/09 | 一致？ |
|------|------|--------------|--------|
| 项目名称 | LibrarySeatSystem | docs/07 §3: `LibrarySeatSystem/` | ✅ |
| 实体命名 | User/Seat/Reservation | docs/08 §2: 同一套 | ✅ |
| HomeController 路由 | `/` `/Home/Seats` `/Home/Detail/{id}` | docs/09 §2.2: 同一套 | ✅ |
| AdminController 路由 | `/Admin/Login` `/Admin/Seats` ... | docs/09 §3.2: 同一套 | ✅ |
| 命名空间 | LibrarySeatSystem.Controllers/Models/Data | docs/07 §3: 同一套 | ✅ |

**注意**：项目名称为 `LibrarySeatSystem`，而非审计路径中提到的 `LibrarySeatReservation.Web`。后者在现有文档中没有任何对应定义，当前项目不做重命名。

### 10. 首页、用户端入口、管理端入口是否可访问？

⚠️ **从代码结构判断可访问，但未实际运行验证。**

- `dotnet build` ✅ 0 errors 0 warnings
- `HomeController.Index` → route `/` ✅
- `AdminController.Login` → route `/Admin/Login` ✅
- 未执行 `dotnet run` + 浏览器打开验证（受限于当前 CLI 环境无浏览器）

### 11. 默认补足项或当前假设是否已显式写出？

⚠️ **部分写出，缺少"为何没有使用 EF Core 迁移"外其他设计决策的显式假设。**

README 的"已知限制"覆盖了运行时限制。`docs/11-开发前一致性总审计.md` §13 给出了修订建议。但关于"为何 Sprint 0 尚未执行"没有说明——当前状态是"设计阶段产物就绪 + 门禁已通过 + 等待 Sprint 0 启动"，应在文档中明确标注。

此外，README 的"当前阶段"中 Sprint 0 标注为"进行中"，而实际任务板显示 Sprint 0 尚未启动，二者存在矛盾。

### 12. 任务板和 Sprint 0 状态是否已更新？

❌ **Sprint 0 状态未更新。** 任务板 §1 "已完成"区已正确记录了 9 条前置修复，但 §2 "Sprint 0" 分区存在以下问题：

| 问题点 | 当前状态 | 期望状态 |
|--------|---------|---------|
| T10-01 ~ T10-09 状态 | 全部"☐ 待开发" | 至少应有启动标记 |
| "任务总数：9 / 已完成：0 / 完成率：0%" | 0% | 应与已完成区匹配 |
| "进行中"列 | 空 | 若 Sprint 0 已启动，应有任务在"进行中" |
| "Sprint 0" 标注 | 第 1 轮 / 共 ? 轮 | 如未启动应标注"待启动" |

### 13. 开发前总审计中的 P0 阻塞项是否确实已清零或已闭环？

✅ **已清零。** docs/11 结论为"无 P0 问题"，4 项 P1 问题（P1-01~P1-04）已记录在任务板中（T10-05、T13-02 已更新验收标准），不阻塞 Sprint 0 启动。

### 14. README 是否已按本轮真实结果增量更新？

⚠️ **部分准确，但存在一处矛盾。**

准确的部分：
- "当前为 Sprint 0 启动前状态" ✅ 描述准确
- ✓/◇ 标注准确 ✅ 已存在 ≠ 待新建 区分清楚
- "已实现范围"段落准确 ✅

矛盾部分：
- "当前阶段"复选框：Sprint 0 标记为"进行中" ❌ 实际应标记为"待启动"

### 15. 是否已经达到"能编译、能启动、能看到入口页、能完成首次建库建表和种子初始化"的目标？

⚠️ **门槛能力具备但 Sprint 0 未执行。**

| 目标 | 能力 | 状态 |
|------|------|------|
| 能编译 | `dotnet build` ✅ 0 errors 0 warnings | ✅ |
| 能启动 | Program.cs 完整，可 `dotnet run` | ✅ |
| 能看到入口页 | 首页 `/` 路由存在 | ✅ |
| 能完成首次建库建表 | `EnsureCreated()` 已配置 | ✅ |
| 能完成种子初始化 | `DbInitializer.Seed()` 已配置 | ✅ |
| Sprint 0 已执行 | T10-01 ~ T10-09 未执行 | ❌ |

### 16. 是否只围绕阶段12的放行条件审计？

✅ **是的。** 本审计仅围绕"开发起步与项目骨架"阶段的放行条件进行。前序文档的细节差异（如 docs/01 与 docs/03 的页面计数差异已在 docs/11 中追溯为正常演进）不扩大审查。所有发现的问题均为当前阶段产物（README、任务板、代码骨架）的实际状态，不涉及对 docs/07/08/09 的重新审计。

---

## C. 发现的问题清单

| 编号 | 类型 | 文件 | 问题 | 严重度 |
|------|------|------|------|--------|
| S01 | 状态不一致 | README.md "当前阶段" | Sprint 0 标记为"进行中"，但实际未启动 | **必改** |
| S02 | 任务未执行 | 任务板 Sprint 0 | T10-01 ~ T10-09 全部"☐ 待开发"，Sprint 0 完成率 0% | **必改** |
| S03 | 假设未说明 | 项目根 | 无文档说明"当前已完成前置修复但 Sprint 0 尚未启动"的阶段性状态 | 建议优化 |
| S04 | 运行未验证 | — | `dotnet run` + 浏览器访问未实际验证 | 建议优化 |

---

## D. 必改项

| 编号 | 位置 | 要求 | 理由 |
|------|------|------|------|
| S01 | README.md "当前阶段" | Sprint 0 复选框改为"待启动"或清空为未勾选状态 | 避免阶段状态误导 |
| S02 | 任务板 Sprint 0 | 启动 Sprint 0，将 T10-01 移入"进行中，或将状态从"待开发"更新为"进行中"标记 | Sprint 0 阶段目标尚未开始执行 |

---

## E. 建议优化项

| 编号 | 建议 | 理由 |
|------|------|------|
| R01 | 在 README 或独立笔记中记录"当前已完成前置修复但 Sprint 0 尚未正式启动"的过渡状态 | 便于后续评审理解当前进度 |
| R02 | 执行一次 `dotnet run` 并确认浏览器可访问首页 200 | 确保运行时无隐藏问题 |
| R03 | T10-02 验收标准⑤（确认三张表）需确认 `EnsureCreated` 已实际生成表结构 | 代码配置正确但不代表数据库已创建 |

---

## F. 是否可以进入下一步"用户端主链路开发"

**No-Go。** 原因：

1. **Sprint 0 未执行。** Sprint 0 的 9 项任务（Service 层、ViewModel、Filter、Controller 重构、前端校验）全部未完成。用户端主链路开发（Sprint 1）的前提是 Service 层可调用、ViewModel 可用、Controller 已重构为 Service 调用模式。当前状态无法直接进入 Sprint 1。

2. **放行条件未满足。** Sprint 0 放行条件为"Service 层可被 Controller 调用 + 任意页面 200"。当前不存在 Service 层。

**推荐下一步：** 执行 Sprint 0（T10-01 ~ T10-09），完成后方可进入 Sprint 1。
