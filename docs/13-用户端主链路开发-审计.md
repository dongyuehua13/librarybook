# 《图书馆座位预约系统》用户端主链路开发 — 审计报告

> 审计日期：2026-07-13  
> 审计对象：README.md、docs/13-用户端主链路开发记录.md、docs/项目任务板与迭代记录.md、src/  
> 对照基线：docs/05、docs/08、docs/09、docs/12  
> 审计范围：仅阶段13新增结果，不重做阶段12或更早阶段的全量审计

---

## A. 总体判断

**未通过 — 文档与代码存在结构性矛盾。**

docs/13、README、任务板三者共同描述了一个 Sprint 1 仅完成 3/8（37.5%）的项目状态，记录的项目路径是 `src/LibrarySeatReservation.Web/`。但仓库根目录下实际可工作的完整项目位于 `LibrarySeatSystem/`，该项目的 Sprint 1（用户端主链路）已全部完成（8/8），Sprint 2 也已全部完成（8/8）。

**核心问题：** 审计路径要求的 `src/` 目录下的 `LibrarySeatReservation.Web/` 是一个未完成的旧骨架项目（Sprint 1 仅 3/8），而所有可工作的完整代码位于 `LibrarySeatSystem/`（Sprint 1 和 2 均为 8/8）。两套项目共存的状态导致本轮审计无法对"用户端主链路开发"给出单一结论——按 `src/` 看是未完成，按实际代码看是已完成。

---

## B. 13 项检查点逐项结果

### 1. 是否优先按照 docs/05/08/09 的页面名、字段名、状态名和交互来实现？

**按实际代码（`LibrarySeatSystem/`）：✅ 完全对齐。**

| 检查项 | docs 规定 | 代码实现 | 结论 |
|--------|----------|---------|------|
| 页面命名 | 9 页，P01~P09 | HomeController.Index/Seats/Detail/Reserve/MyReservations + AdminController.Login/Seats/Reservations/Stats | ✅ |
| 实体字段 | docs/08 §4 | User/Seat/Reservation 字段完全对齐 | ✅ |
| 状态值 | "已预约"/"已取消"，IsActive | 代码中一致使用 | ✅ |
| 路由 | `/Home/Seats` `/Admin/Login` 等 | 完全一致 | ✅ |
| 交互 | AJAX POST JSON / Toast / 确认弹窗 | Reserve/Cancel 均为 AJAX，Toast 代替 alert | ✅ |

**按 `src/LibrarySeatReservation.Web/`：⚠️ 部分实现。** 仅 3 页（首页/座位列表/详情）完成。

### 2. 是否真的形成了完整用户闭环？

**`LibrarySeatSystem/`：✅ 完整。** 首页 → 座位列表（筛选+筛选保留） → 座位详情 → 预约提交（GET 表单 + POST AJAX + 冲突检测） → 我的预约（含 CanCancel） → 取消（AJAX + 确认弹窗 + Toast） → 账号切换/退出。**闭环完整可走通。**

**`src/LibrarySeatReservation.Web/`：❌ 不完整。** 仅完成浏览链路，缺少预约提交、我的预约、取消三个关键环节。

### 3. 列表、详情、提交、我的预约之间是否连通？

**`LibrarySeatSystem/`：✅ 全连通。**
- 首页 → "浏览座位" → `/Home/Seats` ✅
- 座位列表 → "查看详情" → `/Home/Detail/{id}` ✅
- 座位详情 → "预约此座位" → `/Home/Reserve/{seatId}` ✅
- 预约成功 → Toast 含"查看我的预约"链接 → `/Home/MyReservations` ✅
- 我的预约 → "取消" → AJAX POST → 自动刷新 ✅
- 导航栏：浏览座位 / 我的预约 / 切换账号 / 退出 ✅
- 面包屑：首页 > 座位列表 > 座位详情 / 预约提交 ✅

### 4. 是否存在真实数据库写入和读取？

**`LibrarySeatSystem/`：✅ 是。** 全部通过 EF Core + Service 层：
- 写入：`ReservationService.CreateAsync()` → INSERT ✅，`CancelAsync()` → UPDATE ✅
- 读取：`GetSeatsWithStatusAsync()`, `GetSeatDetailAsync()`, `GetUserReservationsAsync()` ✅
- 统计：`GetDashboardStatsAsync()`, `GetAllStatsAsync()` ✅
- 种子数据：`DbInitializer.Seed()` → `EnsureCreated()` → `if (!Users.Any())` 防重复 ✅

### 5. 当前用户 Session 是否真正参与数据归属？

**`LibrarySeatSystem/`：✅ 是。**
- `Session["UserId"]` 在 SwitchUser POST 写入 ✅
- 预约提交：`HttpContext.Session.GetInt32("UserId")` → 作为 `CreateAsync(userId, ...)` 参数 ✅
- 我的预约：`GetUserReservationsAsync(userId)` → 只返回当前用户记录 ✅
- 取消校验：`CancelAsync(reservationId, userId)` → `reservation.UserId != userId` 拦截 ✅
- 管理员端：`Session["AdminId"]` 单独与用户 Session 隔离 ✅

### 6. 表单验证是否生效？

**`LibrarySeatSystem/`：✅ 双层验证。**
- 前端：HTML5 `required` + 时间选择仅整点 08:00~22:00 ✅
- 后端：`startTime >= endTime` → JSON error ✅；`> 4h` → JSON error ✅；冲突检测 3 条规则 ✅；座位不可用检测 ✅；`UserId == null` → "请先选择账号" ✅
- UI 反馈：Toast 显示错误消息，表单保留，按钮重新可用 ✅

### 7. 默认补足项是否已写入 docs/13？

**docs/13 §4：✅ 已写入 6 项默认补足项。** 但补足项记录的是 `src/LibrarySeatReservation.Web/` 的实现细节（如 DashboardStats、Logout 路由、筛选硬编码、占用判定口径），与 `LibrarySeatSystem/` 的实际实现不完全一致。

### 8. 任务板是否真实记录了本轮任务卡、状态流转和完成统计？

**⚠️ 部分是，但存在矛盾。** 任务板 §2 Sprint 1 记录了 3/8 (37.5%)，详细描述了 T11-01~T11-03 的完成情况，站会和评审记录存在。但：
- 任务板所有已完成区也包含了 Sprint 2 全部 8 张卡（T12-01~T12-08），显示 8/8 100%，与 Sprint 1 仅 37.5% 的进度描述自相矛盾
- T11-07 在 §1 已完成区有记录，但在 §2 Sprint 1 分区仍标注"☐ 待开发"
- 状态流转不够清晰：Sprint 2 的 8 张卡出现在已完成区，但从未在 Sprint 2 分区标注为"已启动"

### 9. README 是否已按本轮真实实现增量更新？

**❌ 未反映当前实际代码状态。** README 描述了 `src/LibrarySeatReservation.Web/` 项目：
- "Sprint 1 第 1 轮完成 · 3/8 卡 · 37.5%" → 实际 `LibrarySeatSystem/` 已 8/8
- T11-04~T11-08 标注"计划中/待完成" → 实际已完成
- "首页统计概览（ViewBag → HomeIndexViewModel）" → 实际 Index.cshtml 使用 ViewBag（仅骨架中的 ViewModel 未完全切换）
- 目录结构仍指向 `src/LibrarySeatReservation.Web/` 而非 `LibrarySeatSystem/`

但 README 在**内容撰写质量**上符合要求：有 ✓/◇ 标注、有当前阶段、有运行步骤、有已知限制。问题在于它描述的对象与实际可运行项目不一致。

### 10. 阶段最低完成线是否达到？

**按 `LibrarySeatSystem/`：✅ 已超过最低完成线。** 全部最低完成线条件均已满足：
- 首页显示座位数和今日预约数 ✅
- 座位列表按楼层/区域筛选 ✅
- 座位详情展示预约记录 ✅
- 预约提交做冲突检测并成功写入 ✅
- 我的预约列表显示 CanCancel ✅
- 取消功能可用 ✅
- 账号切换可用 ✅

**按 `src/LibrarySeatReservation.Web/` 和 docs/13 记录：❌ 未达到。** 缺少 T11-04~T11-08，仅满足 3/7 条件。

### 11. Sprint 1 记录是否真实？

**docs/13 §5-§8：有记录但对象错误。** 验证结果（§5）中"首页统计卡片""座位列表筛选""座位详情""空状态""面包屑"等描述与 `LibrarySeatSystem/` 的实现一致，但文件路径全部写为 `src/LibrarySeatReservation.Web/`。站会记录、评审记录、回顾记录的内容本身是合理的，但描述的项目路径不匹配实际可运行代码的路径。

### 12. 是否已经具备进入下一步"管理端、权限与状态回流"的基础？

**按 `LibrarySeatSystem/`：✅ 已充分具备。** 管理端（Sprint 2）的 8 张卡也已全部完成：
- 管理端登录/登出（含已登录自动跳转）✅
- 座位管理（表格 + 行内启用/禁用 AJAX）✅
- 新增/编辑座位表单（含编号查重）✅
- 预约管理（状态+日期筛选 + 空状态）✅
- 统计页（卡片 + 排行 + 趋势 + 空数据状态）✅
- AdminAuthFilter 全页拦截（含 AJAX 401 JSON）✅

**按 `src/LibrarySeatReservation.Web/` 和 docs 记录：❌ 不具备。** Sprint 1 尚未完成。

### 13. 是否把前序文档只当作实现基线，而非扩大成全量返工？

**✅ 是的。** 本审计未对前序文档（docs/05/08/09/12）进行重新全量审查。发现的问题均为当前轮次产物（docs/13、README、任务板）与实际代码之间的偏差。

---

## C. 发现的问题清单

### P0 级（阻塞进入下一步前必须解决）

| 编号 | 文件 | 问题描述 |
|------|------|---------|
| **P0-01** | `docs/13-用户端主链路开发记录.md` | 全程记录的是 `src/LibrarySeatReservation.Web/` 的 3/8 进度，而实际可运行项目 `LibrarySeatSystem/` 的 Sprint 1 已 8/8 完成。需重写 docs/13 使其反映 `LibrarySeatSystem/` 的真实状态 |
| **P0-02** | `docs/项目任务板与迭代记录.md` | Sprint 1 标注 3/8 (37.5%)，Sprint 2 却标注 8/8 (100%)，Sprint 1 尚未完成的情况下 Sprint 2 已完成，进度逻辑自相矛盾 |
| **P0-03** | `README.md` | 描述的项目路径为 `src/LibrarySeatReservation.Web/`，与可运行项目 `LibrarySeatSystem/` 不匹配。Sprint 1 进度标注为 37.5%，实际已 100% |

### P1 级（应在本轮或 Sprint 3 内解决）

| 编号 | 文件 | 问题描述 |
|------|------|---------|
| **P1-01** | `LibrarySeatSystem\Controllers\HomeController.cs:28` | Index Action 仍使用 `ViewBag` 而非 `HomeIndexViewModel`，与 docs/13 声称的"从 ViewBag 重构为 ViewModel"以及 README 描述的"首页统计概览（ViewBag → HomeIndexViewModel）"不一致 |
| **P1-02** | `LibrarySeatSystem\Services\StatsService.cs:34-35` | `GetAllStatsAsync` 中 `todayReservations` 未按 docs/08 §8.1 口径过滤 `Status == "已预约"`，总预约次数也使用了全部记录（含已取消），与设计文档不完全一致 |
| **P1-03** | 任务板 | T13-02/T13-03 仍标注"☐ 待开发"，实际已生成 30 个集成测试代码，需更新状态 |

---

## D. 必改项

| 编号 | 操作 | 理由 |
|------|------|------|
| A01 | **重写 docs/13** 使之反映 `LibrarySeatSystem/` 的真实 Sprint 1 完成状态（8/8）和完整用户闭环 | P0-01 |
| A02 | **统一任务板 Sprint 进度**：Sprint 1 = 8/8 (100%)，Sprint 2 = 8/8 (100%)，Sprint 3 保持当前进度 | P0-02 |
| A03 | **更新 README** 确认项目路径为 `LibrarySeatSystem/`，Sprint 1/2 标注为完成 | P0-03 |
| A04 | **修复 P1-01**：HomeController.Index 改用 HomeIndexViewModel | P1-01 |
| A05 | **修复 P1-02**：GetAllStatsAsync 中今日预约数加 Status 过滤 | P1-02 |

---

## E. 建议优化项

| 编号 | 建议 | 理由 |
|------|------|------|
| R01 | 如 P0-02（项目结构清理）和 P0-03 尚未完成，建议与本轮必改项一并完成 | 多项审计反复指出同一结构问题 |
| R02 | docs/13 补充"对比对照"章节，记录 Sprint 1实现的各页面与 docs/05 原型的差异 | 增强可追溯性 |
| R03 | 任务板 Sprint 3 中 T13-02/T13-03 状态同步为"集成测试代码已完成" | 反映实际进展 |

---

## F. 是否可以进入下一步"管理端、权限与状态回流"

### 按实际代码（`LibrarySeatSystem/`）

**✅ 已可进入。** 实际上 Sprint 2（管理端、权限与状态回流）已在 `LibrarySeatSystem/` 中全部完成（8/8）。当前实际处于 Sprint 3 收尾阶段。

### 按文档记录（README + 任务板 + docs/13）

**❌ 不能。** 文档记录显示 Sprint 1 仅 37.5%，未达到阶段最低完成线。

### 综合建议

**在完成以下清理前，不能进入下一步：**

```
步骤 1: 确认仓库统一项目结构（清理 src/LibrarySeatReservation.Web/ 或确认为弃用）
步骤 2: 重写 docs/13 使其反映 LibrarySeatSystem/ 的真实 Sprint 1 状态（8/8）
步骤 3: 更新 README 和任务板进度（A02/A03）
步骤 4: 修复 P1-01（Index 改用 ViewModel）和 P1-02（Stats 口径过滤）
步骤 5: 完成上述修复后，继续 Sprint 3 剩余任务（T13-06 推送）
```

结束条件：文档与代码不再矛盾，`LibrarySeatSystem/` 的 Sprint 1/2 实现在文档中得到准确反映。
