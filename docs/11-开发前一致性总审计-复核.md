# 《图书馆座位预约系统》开发前一致性总审计 — 复核报告

> 审计对象：docs/开发前一致性总审计.md、docs/项目任务板与迭代记录.md  
> 证据来源：抽样 docs/01 ~ docs/10、README.md、docs/10-开发准备与Sprint0.md  
> 复核方法：8 项检查点逐项评估

---

## A. 总体判断

原审计总体质量良好，覆盖了数据层、架构、业务规则、管理端功能共 4 个维度，发现 10 个真实问题（含必改+建议），结论导向明确。**但存在 1 项事实性误判（C04 依据引用错误）**，且作为"能否进入开发的门禁"尚有 3 个维度覆盖不足。复核定性：原审计 **有条件通过**，修正 C04 误判后可作为进入 Sprint 1 的决策依据。

---

## B. 8 项检查点逐项评审

### 1. 是否覆盖了 01~10 核心文档、README 和任务板？

| 文档 | 审计覆盖情况 | 结果 |
|------|------------|------|
| docs/01 | ❌ 未引用，未参与任何对比 | 缺失 |
| docs/02 | ✅ B3 引用（C09），B5 引用（C12） | 覆盖 |
| docs/03 | ✅ B3 引用（C09），B5 引用（C12） | 覆盖 |
| docs/04 | ⚠️ 仅 C12（页面数）间接涉及，未做路由/跳转对比 | 部分覆盖 |
| docs/05 | ❌ 未引用 | 缺失 |
| docs/06 | ❌ 未引用 | 缺失 |
| docs/07 | ✅ B2 引用（C05~C08） | 覆盖 |
| docs/08 | ✅ B1 全面引用（C01~C04） | 覆盖 |
| docs/09 | ✅ B2/B3/B4 引用（C05~C11） | 覆盖 |
| docs/10 | ✅ B2 备注引用（Sprint 0 计划任务） | 覆盖 |
| README | ✅ B2 备注引用（Sprint 0 待重构说明） | 覆盖 |
| 任务板 | ⚠️ 备注引用但未同步写入阻塞项 | 见 Check 5 |

**结论：** docs/01（立项单）、docs/05（UI规格）、docs/06（原型）未被审计涉及，这三个文档中的约束未得到一致性验证。

---

### 2. 是否真的检查了范围、页面、数据、实现、验收五类一致性？

| 类别 | 检查项 | 审计结果 |
|------|--------|---------|
| **范围** | MVP 功能清单是否在各文档一致？ | ⚠️ 部分 — 检查了页面数（C12），但功能级清单（17 项功能 vs 实际代码）未逐项比对 |
| **页面** | 9 页的路由、职责、跳转是否写代码一致？ | ❌ 未做 — 路由映射、视图文件存在性、导航规则未比对 |
| **数据** | 3 实体字段、类型、约束、关系是否一致？ | ✅ 充分 — C01~C04 逐字段比对 |
| **实现** | 架构模式、API 签名、错误处理是否对齐？ | ✅ 充分 — C05~C11 覆盖了架构/业务/功能 |
| **验收** | docs/03 的 16+3 条验收标准是否可通过？ | ❌ 未做 — 验收标准一致性未被审计 |

**结论：** 缺"验收一致性"和"页面/路由一致性"检查。作为"能否进入开发"的门禁，验收标准可执行性检查尤为重要但被遗漏。

---

### 3. 是否具体指出了跨文档冲突，而不是只做单文档评语？

✅ 总体良好：

| 编号 | 冲突类型 | 参与方 |
|------|---------|--------|
| C09 | 代码 vs 三份文档 | 代码 vs docs/02 + docs/03 + docs/09 |
| C12 | 文档 vs 文档 | docs/02 vs docs/03 |
| C13 | 文档 vs 代码 | docs/09 vs AdminController.cs |
| C10 | 文档 vs 代码 | docs/09 vs AdminController.cs |
| C01~C04 | 文档 vs 代码 | docs/08 vs 代码 |

⚠️ 但有一处 **跨文档冲突被遗漏**：

- docs/07 §7.1 和 docs/08 §8.1 的"今日预约数"统计口径均**不**过滤 Status（`COUNT WHERE Date = today`）
- docs/09 §2.4 的"今日预约数"**过滤** Status（`Status == "已预约"`）
- 原审计 C04 把 docs/08 当作"需要加 Status 过滤"的依据，但 docs/08 的 SQL 实际上**不**需要过滤

---

### 4. P0 / P1 分级是否合理？

| 编号 | 原定级 | 复核定级 | 理由 |
|------|--------|---------|------|
| C01 | P0 | **P0** | 唯一索引缺失可导致重复 username，数据完整性 | 
| C02 | P0 | **P0** | Cascade 删除可意外清除预约历史，数据安全 |
| C04 | P0 | **→ 降为 P1** | 仅影响首页展示数字（显示总量而非有效量），无数据丢失风险；且有 docs/08 支持旧定义 |
| C09 | P0 | **→ 降为 P1** | 功能正常运作，仅文档滞后于代码，不阻塞开发 |
| C10 | P0 | **P0** | 无查重可产生重复座位编号，影响核心业务 |

**结论：** C04 和 C09 不应列为 P0（阻塞级），应下调为 P1（重要但不阻塞）。

---

### 5. 阻塞项是否已经同步写进任务板？

**❌ 未同步。** 原审计发现后直接在代码中修复了全部问题（见 F 节修复结果），但：
- 任务板上 Sprint 0 的 9 张任务卡均未被**更新状态**（所有卡仍为"☐ 待开发"）
- 审计发现的 C01/C02/C04/C10 未作为独立阻塞项写入任务板的"本轮新增完成卡片"
- C09 的"更新文档"项未出现在任何任务卡中
- 任务板的"已完成"区域仍为空

正确做法应是：将每个阻塞项创建为任务卡 → 移入"进行中" → 修复后移入"已完成"。

---

### 6. Go / No-Go 结论是否有证据支撑？

⚠️ **部分有支撑，但 C04 证据引用错误。**

| 结论 | 支撑证据 | 是否充分 |
|------|---------|---------|
| "全部必改项已修复" | F 节列出 9 个修复 + `dotnet build` 验证 | ✅ 有代码行号、有构建结果 |
| "可以启动 Sprint 1" | 全部代码修改 + 文档更新完成 | ⚠️ 但 C04 修复方向为"加 Status 过滤"，而 docs/08 §8.1 不需要该过滤，需确认意图 |
| C01 依据 docs/08 §4.4 | 文档截图 + 代码行号 | ✅ |
| C04 依据 docs/08 §8.1 | 声称 docs/08 要求 Status 过滤 | ❌ **事实错误** — docs/08 §8.1 的 SQL 为 `WHERE Date = today`，**无** Status 过滤 |

---

### 7. 是否已经足以作为"能不能进入开发"的门禁结果？

**基本可以，但需修正 C04 后重新确认。**

原审计覆盖了数据层（充分）、业务规则（良好）、管理端功能（良好）、架构模式（识别为计划债务）。缺少的维度（验收标准、页面路由、UI/UX 一致性）对于单人课堂项目的门槛来说可以接受，不会导致开发阶段出现结构性障碍。

修正后的障碍清单（5 项原 P0 中实际应保留 3 项为 P0）：

| 实际 P0 | 内容 | 状态 |
|---------|------|------|
| C01 | Username 唯一索引 | ✅ 已修复 |
| C02 | FK 的 NoAction | ✅ 已修复 |
| C10 | SeatCreate 编号查重 | ✅ 已修复 |

以上 3 项修复后，其余项目（C04 降为 P1、C09 降为 P1）可进入 Sprint 1 并行修复。

---

### 8. 是否错误地把 README 或 docs/10 按最终仓库形态审阅？

**未发现此类错误。** 

原审计对 C05~C08（缺少 Service/ViewModel/Filters）的处理是正确的：

> C05-C08 已在 README 和任务板 Sprint 0（T10-05 ~ T10-09）明确标注为待重构项。**不视为本次审计的缺陷**，但需在 Sprint 0 完成后消除差异。

这表明审计者正确理解了当前阶段（Sprint 0 启动前）和最终形态的差异，没有把尚未生成的文件误判为冲突。✅

---

## C. 原审计的事实性修正

### 修正 1：C04 依据错误 + 修复方向需要确认

| 项目 | 内容 |
|------|------|
| 原审计 C04 主张 | "docs/08 §8.1 指定 `COUNT WHERE Date=today AND Status='已预约'`" |
| 事实 | docs/08 §8.1 的 SQL 为：`SELECT COUNT(*) FROM Reservations WHERE Date = CAST(GETDATE() AS DATE)`，**无** Status 过滤 |
| 实际冲突 | docs/07 §7.1 (无过滤) + docs/08 §8.1 (无过滤) **vs** docs/09 §2.4 (有过滤 `Status == "已预约"`) |
| 建议 | 需明确"今日预约数"的口径：(A) 当日所有预约记录总数（含已取消），对齐 docs/07+08；或 (B) 当日有效预约数（仅已预约），对齐 docs/09。**建议选 B**（首页展示有效预约更合理），然后同步修正 docs/07 §7.1 和 docs/08 §8.1 的 SQL |

### 修正 2：docs/01 页面数未同步

R04 只修正了 docs/02 的页面数，但 docs/01 §7 仍写"一共 10 个页面"。需补充同步。

---

## D. 任务板状态缺口

当前任务板 Sprint 0 全部 9 张卡的当前状态仍为"☐ 待开发"。已完成的实际工作（AppDbContext 配置修正、HomeController 统计口径、AdminController 查重/编辑、文档更新等）未在任务板上留下任何记录。建议补录以下已完成项至任务板的"已完成"区域：

| 卡片编号 | 卡片标题 | 归属 Sprint |
|---------|---------|------------|
| T10-03 子项 | AppDbContext 补 Username 唯一索引 + FK NoAction | Sprint 0 |
| T10-04 子项 | HomeController 首页统计口径修正 | Sprint 0 |
| T10-05 子项 | AdminController.SeatCreate 编号查重 | Sprint 0 |
| T10-05 子项 | AdminController.SeatEdit 逐字段更新 + 编号查重 | Sprint 0 |
| T10-05 子项 | AdminController.Reservations 参数名统一 | Sprint 0 |
| — | Reservation.Date 加 [Column(TypeName = "date")] | Sprint 0 |
| — | docs/03 F4 验收标准增补 3 条规则 | Sprint 0 |
| — | docs/09 §2.3/§2.4/§2.8 增补校验规则 | Sprint 0 |
| — | docs/02 §5.1 页面数统一为 9 页 | Sprint 0 |

---

## E. 是否可以进入下一步"开发起步与项目骨架"

**可以。** 但建议在 Sprint 1 启动前完成以下 3 项：

1. **C04 口径确认** — 在口径 A（总数）和口径 B（有效数）之间选定，同步修正 docs/07、docs/08
2. **docs/01 页面数同步** — 将"10 个页面"改为"9 个页面"
3. **任务板补录** — 将上述已完成工作记入"已完成"区域，使状态可追溯

以上不阻塞开发起步，可在 Sprint 1（T11-01 首页实现）启动后同步完成。建议优先级：C04 口径确认 > 任务板补录 > docs/01 同步。
